"""M01 Starlark testing facade (ADR 0009)."""

load("//quality:fixtures.bzl", "real_source_target")

exports_files([
    "behavioral_matrix.md",
    "defs.bzl",
])

# Repository corpus (M05 WP1): facade Starlark and matrix Markdown owned
# for direct-Bazel dogfood under the canonical real_fixture_policy.
real_source_target(
    name = "corpus",
    aspect_hints = ["//quality:corpus_vale_config"],
    markdown_srcs = ["behavioral_matrix.md"],
    starlark_srcs = [
        "BUILD.bazel",
        "defs.bzl",
    ],
)
