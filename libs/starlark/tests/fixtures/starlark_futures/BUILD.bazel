"""Starlark testing futures fixture."""

load("//quality:fixtures.bzl", "real_source_target")

package(
    default_testonly = True,
    default_visibility = ["//:__subpackages__"],
)

exports_files([
    "aspect_subjects.bzl",
    "matchers.bzl",
    "output_group_subjects.bzl",
    "pins.bzl",
    "starlark_futures.expected",
    "toolchain_subjects.bzl",
])

real_source_target(
    name = "corpus_starlark",
    testonly = True,
    starlark_srcs = [
        "BUILD.bazel",
        "aspect_subjects.bzl",
        "matchers.bzl",
        "output_group_subjects.bzl",
        "pins.bzl",
        "toolchain_subjects.bzl",
    ],
    tags = ["corpus"],
)
