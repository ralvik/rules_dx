load("@bazel_skylib//rules:common_settings.bzl", "string_flag")
load("//quality:fixtures.bzl", "real_source_target")

package(
    default_testonly = True,
    default_visibility = ["//:__subpackages__"],
)

exports_files([
    "action_subjects.bzl",
    "aspect_subjects.bzl",
    "config_subjects.bzl",
    "matchers.bzl",
    "output_group_subjects.bzl",
    "pins.bzl",
    "starlark_futures.expected",
    "toolchain_subjects.bzl",
])

string_flag(
    name = "config_value",
    build_setting_default = "plain",
    values = [
        "flipped",
        "plain",
    ],
)

config_setting(
    name = "is_flipped",
    flag_values = {":config_value": "flipped"},
)

real_source_target(
    name = "corpus_starlark",
    testonly = True,
    aspect_hints = ["//:buildifier_config"],
    starlark_srcs = [
        "BUILD.bazel",
        "action_subjects.bzl",
        "aspect_subjects.bzl",
        "config_subjects.bzl",
        "matchers.bzl",
        "output_group_subjects.bzl",
        "pins.bzl",
        "toolchain_subjects.bzl",
    ],
    tags = ["corpus"],
)
