"""M06 WP3 apply boundary for the `dx` CLI."""

load("@rules_rust//rust:defs.bzl", "rust_clippy_test", "rustfmt_test")
load("//quality:fixtures.bzl", "real_source_target")
load("//rust/rules:defs.bzl", "dx_rust_library", "dx_rust_test")

package(default_visibility = ["//visibility:public"])

dx_rust_library(
    name = "dx_apply",
    srcs = [
        "src/applier.rs",
        "src/consensus.rs",
        "src/envelope.rs",
        "src/lib.rs",
        "src/validators.rs",
    ],
    crate_name = "dx_apply",
    edition = "2021",
    deps = [
        "@crates//:serde",
        "@crates//:serde_json",
        "@crates//:sha2",
    ],
)

dx_rust_test(
    name = "dx_apply_test",
    size = "small",
    crate = ":dx_apply",
    edition = "2021",
)

# Authoritative quality identities (M02 pattern): rustfmt and Clippy
# resolve from the pinned toolchain, never from ambient host tools.
rustfmt_test(
    name = "dx_apply_fmt_test",
    size = "small",
    targets = [":dx_apply"],
)

rust_clippy_test(
    name = "dx_apply_clippy_test",
    size = "small",
    targets = [":dx_apply"],
)

exports_files(["Cargo.toml"])

# Repository corpus (M05 WP1): crate manifest and build file owned for
# direct-Bazel dogfood under the canonical real_fixture_policy.
real_source_target(
    name = "corpus",
    starlark_srcs = ["BUILD.bazel"],
    toml_srcs = ["Cargo.toml"],
)
