[package]
name = "dx_audit"
version = "0.0.0"
edition = "2021"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["alloc"] }
hex = "0.4"
semver = "1"
spdx = "0.13"
thiserror = "2"
url = "2"

[lib]
name = "dx_audit"
path = "src/lib.rs"
