"""M30b slice 1: pure adoption-scaffolding planning (dx init absent-only writes, single-version pin and rollback)."""

load("@crates//:crates.bzl", "aliases")
load("@rules_rust//rust:defs.bzl", "rust_clippy_test", "rustfmt_test")
load("//quality:fixtures.bzl", "real_source_target")
load("//rust/rules:defs.bzl", "dx_rust_library", "dx_rust_test")

package(default_visibility = ["//visibility:public"])

dx_rust_library(
    name = "dx_adopt",
    srcs = ["src/lib.rs"],
    aliases = aliases(
        package_name = "dx/dx_adopt",
        normal = True,
    ),
    crate_name = "dx_adopt",
    crate_root = "src/lib.rs",
    edition = "2021",
)

dx_rust_test(
    name = "dx_adopt_test",
    size = "small",
    aliases = aliases(
        package_name = "dx/dx_adopt",
        normal = True,
        normal_dev = True,
    ),
    crate = ":dx_adopt",
    edition = "2021",
)

# Authoritative quality identities (M02 pattern): rustfmt and Clippy
# resolve from the pinned toolchain, never from ambient host tools.
rustfmt_test(
    name = "dx_adopt_fmt_test",
    size = "small",
    targets = [":dx_adopt"],
)

rust_clippy_test(
    name = "dx_adopt_clippy_test",
    size = "small",
    targets = [":dx_adopt"],
)

exports_files(["Cargo.toml"])

# Repository corpus (M05 WP1): crate manifest and build file owned for
# direct-Bazel dogfood under the canonical real_fixture_policy.
real_source_target(
    name = "corpus",
    starlark_srcs = ["BUILD.bazel"],
    toml_srcs = ["Cargo.toml"],
)
