[package]
name = "dx_adopt"
version = "0.0.0"
edition = "2021"

[lib]
name = "dx_adopt"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tempfile = "3"
