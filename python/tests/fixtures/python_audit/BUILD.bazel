"""Python source-audit split fixture."""

load("@rules_dx//python/rules:defs.bzl", "python_library")
load("//quality:fixtures.bzl", "real_source_target")

package(
    default_testonly = True,
    default_visibility = ["//:__subpackages__"],
)

exports_files([
    "pins.bzl",
    "python_audit.expected",
    "Sample_audit_clean.py",
    "Sample_audit_dirty.py",
])

real_source_target(
    name = "corpus_starlark",
    testonly = True,
    aspect_hints = ["//:buildifier_config"],
    starlark_srcs = [
        "BUILD.bazel",
        "pins.bzl",
    ],
    tags = ["corpus"],
)

python_library(
    name = "Sample_audit_clean",
    testonly = True,
    srcs = ["Sample_audit_clean.py"],
    imports = ["."],
)

python_library(
    name = "Sample_audit_dirty",
    testonly = True,
    srcs = ["Sample_audit_dirty.py"],
    imports = ["."],
    tags = ["no-lint"],
)
