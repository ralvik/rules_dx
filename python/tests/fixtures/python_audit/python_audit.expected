Python source-audit selection qualified seed-only under issue #801.

Successor to closed #613 (seed-only empty curated audit); taxonomy
under issue #512 stays taxonomy-only. Selected tool is Ruff S
(flake8-bandit) via the pinned Ruff 0.16.7 standalone artifact with
no new acquisition; S ruleset via native ruff.toml opt-in with pinned
upstream defaults otherwise clean and no hidden preset. Curated audit
stays empty with explicit disablement for the python family (audit
opt-in, no default fetch); Bandit excluded from v1 by ADR 0019
(re-selection rejected here, reconsideration under #970); secrets ride
Gitleaks detect with redact plus SARIF. Ruff audit claims python plus
python_stub check-only via the hermetic ruff check JSON path shared
with lint. Python lint stays pydoclint plus ruff, format stays ruff,
typecheck stays ty; flake8 plus pylint stay baseline opt-ins.
Per-language source audit stays distinct from ecosystem dx audit plus
dx update live execution delivered repo-wide. Leaving under taxonomy
rejected with mismatched scope. Platform rides Ruff per-host
artifacts (linux_x86_64 plus linux_arm64 plus macos_arm64 plus
macos_x86_64 plus windows_x86_64); consumer rides adopt-python with
lazy opt-in; release rides promotion-checklist plus SBOM plus signing
linkage. Future selection owned under #801; taxonomy #512 plus
promotion #808 plus per-host #803 through #807 stay owned gaps;
backends provisional; no Supported claim.
