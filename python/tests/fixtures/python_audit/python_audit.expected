Python source-audit split qualified seed-only under issue #613.

Taxonomy under issue #512 stays taxonomy-only; Python source-audit
tooling carries its own tracker here. Curated audit stays empty with
explicit disablement for the python family; Bandit excluded from v1
by ADR 0019; secrets ride Gitleaks detect with redact plus SARIF.
Python lint stays pydoclint plus ruff, format stays ruff, typecheck
stays ty; flake8 plus pylint stay baseline opt-ins. No audit adapter
claims python plus python_stub. Per-language source audit stays
distinct from ecosystem dx audit plus dx update live execution
delivered repo-wide. Leaving under taxonomy rejected with mismatched
scope. Future tool selection stays owned under issue #613; platform
plus consumer plus release owned gap; backends provisional; no
Supported claim.
