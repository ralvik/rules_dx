"""Fantomas format fixture."""

load("@rules_dx//fsharp/rules:defs.bzl", "fsharp_library")
load("//quality:fixtures.bzl", "real_source_target")

package(
    default_testonly = True,
    default_visibility = ["//:__subpackages__"],
)

exports_files([
    "pins.bzl",
    "Sample.fs",
    "check_dirty.json",
])

real_source_target(
    name = "corpus_starlark",
    testonly = True,
    starlark_srcs = [
        "BUILD.bazel",
        "pins.bzl",
    ],
    tags = ["corpus"],
)

fsharp_library(
    name = "fantomas",
    testonly = True,
    srcs = ["Sample.fs"],
)
