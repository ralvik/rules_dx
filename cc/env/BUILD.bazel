"""Focused C/C++ environment plans.

Each `cc_env_plan` reads the analyzed authoritative providers of one
`cc_*` wrapper: `QualitySourcesInfo` for direct sources plus the
preserved `CcInfo` transitive header closure and source/test split.
Focused-target only; repository/root/exact-target orchestration remains.
"""

load("//cc/env:plan.bzl", "cc_env_plan")
load("//cc/env:plan_tests.bzl", "env_plan_tests")
load("//quality:fixtures.bzl", "real_source_target")

package(default_visibility = ["//visibility:private"])

cc_env_plan(
    name = "hello_lib_plan",
    testonly = True,
    target = "//cc/tests/fixtures/hello:hello_lib",
)

cc_env_plan(
    name = "hello_plan",
    testonly = True,
    target = "//cc/tests/fixtures/hello:hello",
)

cc_env_plan(
    name = "hello_test_plan",
    testonly = True,
    target = "//cc/tests/fixtures/hello:hello_test",
)

cc_env_plan(
    name = "hello_c_lib_plan",
    testonly = True,
    target = "//cc/tests/fixtures/hello:hello_c_lib",
)

cc_env_plan(
    name = "hello_c_test_plan",
    testonly = True,
    target = "//cc/tests/fixtures/hello:hello_c_test",
)

env_plan_tests(
    name = "env_plan_tests",
    subjects = [
        ":hello_lib_plan",
        ":hello_plan",
        ":hello_test_plan",
        ":hello_c_lib_plan",
        ":hello_c_test_plan",
    ],
)

real_source_target(
    name = "corpus_starlark",
    starlark_srcs = [
        "BUILD.bazel",
        "plan.bzl",
        "plan_tests.bzl",
    ],
    tags = ["corpus"],
)
