"""Experimental minimal C/C++ wrappers (M22, O30)."""

load("//quality:fixtures.bzl", "real_source_target")

package(default_visibility = ["//visibility:public"])

exports_files([
    "defs.bzl",
])

# Repository corpus: wrapper Starlark owned for direct-Bazel
# dogfood under the canonical real_fixture_policy. C/C++ sources stay
# dx-owned (M22 wrappers), never corpus sources.
real_source_target(
    name = "corpus",
    starlark_srcs = ["BUILD.bazel"] + glob(["*.bzl"]),
)
