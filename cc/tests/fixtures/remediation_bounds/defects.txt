# Remediation defect inventory (issue #505).
# Every source-derived failure path is reproduced with its source,
# estimate, owner, tracking, and complete-workflow evidence. PIE plus
# ELF-dependency plus glibc-symbol uses existing upstream constraints
# with no new infra.

kept CC opt-out linker failure path with sysroot rust-lld fallback plus no_cc stubs
cargo/private/cargo_build_script.bzl no-linker failure path
rust/tests/fixtures/cc_optout/ via cc_optout_qualification under issue #471
Windows transport rebasing with /external:I plus .lib plus .obj bare-path handling
cargo/private/cargo_build_script.bzl /imsvc-only baseline plus batch wrappers plus response files plus spaces plus SDK libraries plus cc-rs discovery plus cc-rs assembly plus proc-macro DLLs
cc/tests/fixtures/windows_transport/pins.bzl via windows_transport_qualification under issue #497
Windows ABI constraint fix so toolchains_msvc cannot satisfy GNU plus GNULVM targets
private/msvc_toolchains_repo.bzl plus rs/platforms/triples.bzl LLVM MSVC ABI identity
windows_transport_qualification under issue #497 with GNU plus GNULVM rejected
Windows acquisition fixed-manifest plus package-index inputs
private/vs_channel_manifest.bzl live VS channel manifests plus minor-version selectors
cc/tests/fixtures/windows_acquisition/pins.bzl via windows_acquisition_qualification under issue #495
Coverage workaround with Bazel 9 LLVM flags plus raw-output workaround plus llvm-cov plus llvm-profdata selection
hermetic-llvm e2e rules_cc .bazelrc plus unreleased rules_cc fix plus rules_rs toolchain without override parameters plus profiler-builtins
cc/tests/fixtures/lcov_accounting/pins.bzl via lcov_accounting_qualification under issue #501
Generation strictness with gazelle_cc resolve plus module index plus test grouping plus generated headers plus assembly plus modules plus PCH
language/cc/resolve.go quoted plus angle plus ambiguity plus warn-and-omit
cc/tests/fixtures/strict_generation/pins.bzl via strict_generation_qualification under issue #503
IDE snapshot adaptation with action-derived commands plus managed clangd plus generated-output materialization plus multi-context headers
refresh.template.py Bazel plus preprocessors plus workspace writes plus continued-after-failures
exact-target discovery qualified seed-only under issue #475 plus C++ snapshot qualified seed-only under issue #754 via cpp_snapshot_qualification
PIE plus ELF-dependency plus glibc-symbol uses existing upstream constraints
PIE qualification uses existing upstream constraints
glibc 2.28 symbol floor plus musl 1.2.6 static closure plus deployment 14.0 qualified seed-only under issue #500
Cross-product expansion only after the initial cohort passes with bounded upstream configuration
cc/tests/fixtures/cross_routes/pins.bzl via cross_routes_qualification under issue #504
the affected capability fails closed until a compliant remedy passes the required evidence
