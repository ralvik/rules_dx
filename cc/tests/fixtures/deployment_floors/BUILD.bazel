"""Deployment plus execution floors fixture (issue #500)."""

load("//quality:fixtures.bzl", "real_source_target")

package(
    default_testonly = True,
    default_visibility = ["//:__subpackages__"],
)

exports_files([
    "apple_frameworks.txt",
    "current_host.txt",
    "floors.expected",
    "loader_deps.txt",
    "oldest_target.txt",
    "pins.bzl",
])

real_source_target(
    name = "corpus_starlark",
    testonly = True,
    starlark_srcs = [
        "BUILD.bazel",
        "pins.bzl",
    ],
    tags = ["corpus"],
)
