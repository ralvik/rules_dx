# Windows transport cases (issue #497).
# Each line names one declared-input fixture the harness checks without
# host Visual Studio state. Compiler-target availability alone is not proof.
batch wrappers
response files
spaces
SDK system libraries
cc-rs discovery
cc-rs assembly tools
proc-macro DLLs
