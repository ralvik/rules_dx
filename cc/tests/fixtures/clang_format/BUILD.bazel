"""Clang-format format fixture."""

load("@rules_dx//cc/rules:defs.bzl", "cc_library")
load("//quality:fixtures.bzl", "real_source_target")

package(
    default_testonly = True,
    default_visibility = ["//:__subpackages__"],
)

exports_files([
    "pins.bzl",
    "Sample.c",
    "check_dirty.diff",
])

real_source_target(
    name = "corpus_starlark",
    testonly = True,
    starlark_srcs = [
        "BUILD.bazel",
        "pins.bzl",
    ],
    tags = ["corpus"],
)

cc_library(
    name = "clang_format",
    testonly = True,
    srcs = ["Sample.c"],
)
