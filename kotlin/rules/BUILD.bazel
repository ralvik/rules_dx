
load("@rules_kotlin//kotlin:core.bzl", "kt_kotlinc_options")
load("//quality:fixtures.bzl", "real_source_target")
load(":wrapper_tests.bzl", "kotlin_wrapper_contract_tests")

package(default_visibility = ["//visibility:public"])

kt_kotlinc_options(
    name = "warnings_as_errors",
    api_version = "2.2",
    language_version = "2.2",
    warn = "error",
)

exports_files([
    "defs.bzl",
    "wrapper_tests.bzl",
])

kotlin_wrapper_contract_tests(name = "wrapper_contract_tests")

real_source_target(
    name = "corpus_starlark",
    aspect_hints = ["//:buildifier_config"],
    starlark_srcs = [
        "BUILD.bazel",
        "defs.bzl",
        "wrapper_tests.bzl",
    ],
    tags = ["corpus"],
)
