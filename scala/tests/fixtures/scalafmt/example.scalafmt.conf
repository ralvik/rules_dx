# Example checked-in Scalafmt native config.
#
# Sole-policy example: without a checked-in `.scalafmt.conf` the pinned tool
# uses upstream built-in defaults; with this file it interprets natively.
version = "3.11.4"
runner.dialect = scala213
