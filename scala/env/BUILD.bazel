"""Focused Scala environment plans.

Each `scala_env_plan` reads the analyzed authoritative providers of one
`scala_*` wrapper: `QualitySourcesInfo` for direct sources plus the
preserved `JavaInfo` transitive source-jar closure and source/test split.
Focused-target only; repository/root/exact-target orchestration remains.
"""

load("//quality:fixtures.bzl", "real_source_target")
load("//scala/env:plan.bzl", "scala_env_plan")
load("//scala/env:plan_tests.bzl", "env_plan_tests")

package(default_visibility = ["//visibility:private"])

scala_env_plan(
    name = "hello_lib_plan",
    testonly = True,
    target = "//scala/tests/fixtures/hello:hello_lib",
)

scala_env_plan(
    name = "hello_plan",
    testonly = True,
    target = "//scala/tests/fixtures/hello:hello",
)

scala_env_plan(
    name = "hello_test_plan",
    testonly = True,
    target = "//scala/tests/fixtures/hello:hello_test",
)

env_plan_tests(
    name = "env_plan_tests",
    subjects = [
        ":hello_lib_plan",
        ":hello_plan",
        ":hello_test_plan",
    ],
)

real_source_target(
    name = "corpus_starlark",
    starlark_srcs = [
        "BUILD.bazel",
        "plan.bzl",
        "plan_tests.bzl",
    ],
    tags = ["corpus"],
)
