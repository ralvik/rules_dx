# Mixed hello example

Vue, Svelte, Astro, and MDX containers over one shared JavaScript helper.

```sh
bazel build //examples/mixed/hello/...
bazel test //examples/mixed/hello/...
```

`hello_test` covers the shared helper edge.
