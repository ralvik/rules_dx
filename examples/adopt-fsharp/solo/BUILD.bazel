"""Adopted foreign F# tree (#193): handwritten owners preserved through generation."""

load("@rules_dx//fsharp/rules:defs.bzl", "fsharp_library", "fsharp_test")
load("//quality:fixtures.bzl", "real_source_target")

package(default_visibility = ["//visibility:public"])

# Handwritten: the generator never emits fsharp_test; test-owned `*Test.fs`
# stays out of the generated library and this rule survives regeneration.
fsharp_test(
    name = "pure_test",
    srcs = ["PureTest.fs"],
    deps = [
        ":solo",
        "@paket.main//fsharp.core",
    ],
)

real_source_target(
    name = "corpus",
    starlark_srcs = ["BUILD.bazel"],
)

fsharp_library(
    name = "solo",
    srcs = ["Pure.fs"],
)
