"""Adopted foreign F# tree: handwritten owners preserved through generation."""

load("@rules_dx//fsharp/rules:defs.bzl", "fsharp_library", "fsharp_test")
load("//quality:fixtures.bzl", "real_source_target")

package(default_visibility = ["//:__subpackages__"])

fsharp_test(
    name = "pure_test",
    srcs = ["PureTest.fs"],
    deps = [
        ":solo",
        "@paket.main//fsharp.core",
    ],
)

fsharp_library(
    name = "solo",
    srcs = ["Pure.fs"],
    deps = ["@paket.main//fsharp.core"],  # keep
)

real_source_target(
    name = "corpus_starlark",
    aspect_hints = ["//:buildifier_config"],
    starlark_srcs = ["BUILD.bazel"],
    tags = ["corpus"],
)
