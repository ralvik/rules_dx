"""Adopted foreign Java tree: handwritten owners preserved through generation."""

load("@rules_dx//java/rules:defs.bzl", "java_library", "java_test")
load("//quality:fixtures.bzl", "real_source_target")

package(default_visibility = ["//:__subpackages__"])

# Handwritten: the generator never emits java_test; test-owned `*Test.java`
# stays out of the generated library and this rule survives regeneration.
java_test(
    name = "greet_test",
    srcs = ["GreetTest.java"],
    test_class = "greet.GreetTest",
    deps = [":greet"],
)

java_library(
    name = "greet",
    srcs = [
        "Greet.java",
        "Helper.java",
    ],
)

real_source_target(
    name = "corpus_starlark",
    starlark_srcs = ["BUILD.bazel"],
    tags = ["corpus"],
)
