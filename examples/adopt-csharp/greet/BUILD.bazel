"""Adopted foreign C# tree: handwritten owners preserved through generation."""

load("@rules_dx//csharp/rules:defs.bzl", "csharp_library", "csharp_test")
load("//quality:fixtures.bzl", "real_source_target")

package(default_visibility = ["//:__subpackages__"])

# Handwritten: the generator never emits csharp_test; test-owned `*Test.cs`
# stays out of the generated library and this rule survives regeneration.
csharp_test(
    name = "greet_test",
    srcs = ["GreetTest.cs"],
    deps = [
        ":greet",
        "@paket.main//xunit.v3.assert",
    ],
)

csharp_library(
    name = "greet",
    srcs = [
        "Greet.cs",
        "Helper.cs",
    ],
)

real_source_target(
    name = "corpus_starlark",
    starlark_srcs = ["BUILD.bazel"],
    tags = ["corpus"],
)
