load("@crates//:crates.bzl", "aliases")
load("@rules_dx//rust/rules:defs.bzl", "rust_binary")
load("//quality:fixtures.bzl", "real_source_target")

rust_binary(
    name = "worker",
    srcs = ["src/main.rs"],
    aliases = aliases(
        package_name = "examples/adopt-rust/crates/worker",
        normal = True,
    ),
    crate_name = "worker",
    crate_root = "src/main.rs",
    deps = ["//examples/adopt-rust/crates/api"],
)

real_source_target(
    name = "corpus",
    starlark_srcs = ["BUILD.bazel"],
    toml_srcs = ["Cargo.toml"],
)
