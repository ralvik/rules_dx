load("@crates//:crates.bzl", "aliases")
load("@rules_dx//rust/rules:defs.bzl", "rust_library", "rust_test")
load("//quality:fixtures.bzl", "real_source_target")

rust_library(
    name = "native",
    srcs = ["src/lib.rs"],
    aliases = aliases(
        package_name = "examples/adopt-polyglot/native",
        normal = True,
    ),
    crate_name = "native",
    crate_root = "src/lib.rs",
    visibility = ["//visibility:public"],
)

rust_test(
    name = "native_test",
    aliases = aliases(
        package_name = "examples/adopt-polyglot/native",
        normal = True,
        normal_dev = True,
    ),
    crate = ":native",
)

real_source_target(
    name = "corpus",
    starlark_srcs = ["BUILD.bazel"],
    toml_srcs = ["Cargo.toml"],
)
