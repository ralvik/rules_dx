load("@rules_dx//python/rules:defs.bzl", "python_library", "python_test")
load("//quality:fixtures.bzl", "real_source_target")

python_library(
    name = "shapes",
    srcs = ["shapes.py"],
    imports = ["."],
)

python_test(
    name = "shapes_test",
    srcs = ["shapes_test.py"],
    # User-owned deps (# keep); see docs/contributing/build-conventions.md.
    dep_group = "hello",
    imports = ["."],
    deps = [
        ":shapes",
        "@pypi//coverage",  # keep
        "@pypi//pytest",  # keep
    ],
)

real_source_target(
    name = "corpus_starlark",
    aspect_hints = ["//:buildifier_config"],
    starlark_srcs = ["BUILD.bazel"],
    tags = ["corpus"],
)
