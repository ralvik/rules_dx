"""Adopted foreign Scala tree: handwritten owners preserved through generation."""

load("@rules_dx//scala/rules:defs.bzl", "scala_library", "scala_test")
load("//quality:fixtures.bzl", "real_source_target")

package(default_visibility = ["//:__subpackages__"])

# Handwritten: the generator never emits scala_test; test-owned `*Test.scala`
# stays out of the generated library and this rule survives regeneration.
scala_test(
    name = "pure_test",
    srcs = ["PureTest.scala"],
    deps = [":solo"],
)

scala_library(
    name = "solo",
    srcs = ["Pure.scala"],
)

real_source_target(
    name = "corpus_starlark",
    aspect_hints = ["//:buildifier_config"],
    starlark_srcs = ["BUILD.bazel"],
    tags = ["corpus"],
)
