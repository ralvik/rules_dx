load("@rules_dx//python/rules:defs.bzl", "python_library", "python_test")
load("//quality:fixtures.bzl", "real_source_target")

python_library(
    name = "pure",
    srcs = ["pure.py"],
    imports = ["."],
)

python_test(
    name = "pure_test",
    srcs = ["pure_test.py"],
    # User-owned runtime edges: see app/BUILD.bazel.
    dep_group = "hello",
    imports = ["."],
    deps = [
        ":pure",
        "@pypi//coverage",  # keep
        "@pypi//pytest",  # keep
    ],
)

real_source_target(
    name = "corpus_starlark",
    starlark_srcs = ["BUILD.bazel"],
    tags = ["corpus"],
)
