"""Adopted foreign Python tree: corpus ownership for arrival files."""

load("//quality:fixtures.bzl", "real_source_target")

package(default_visibility = ["//:__subpackages__"])

# Arrival files; generator-owned BUILD files, regen must be no-op; see docs/generation/README.md.
# Corpus: root arrival files only; see docs/contributing/build-conventions.md.
real_source_target(
    name = "corpus_markdown",
    aspect_hints = ["//quality:corpus_vale_config"],
    markdown_srcs = ["README.md"],
    tags = ["corpus"],
)

real_source_target(
    name = "corpus_starlark",
    starlark_srcs = ["BUILD.bazel"],
    tags = ["corpus"],
)

real_source_target(
    name = "corpus_toml",
    tags = ["corpus"],
    toml_srcs = ["pyproject.toml"],
)

# Per-adopt arrival lock (issue #1077): foreign uv.lock resolved from this
# tree's pyproject.toml (`uv lock`); the Bazel graph keeps using the shared
# hello uv hub, this pair proves foreign consistency plus fail-closed repin
# via //tools/depcheck:adopt_locks_test.
exports_files([
    "pyproject.toml",
    "uv.lock",
])
