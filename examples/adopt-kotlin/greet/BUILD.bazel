"""Adopted foreign Kotlin tree: handwritten owners preserved through generation."""

load("@rules_dx//kotlin/rules:defs.bzl", "kotlin_library", "kotlin_test")
load("//quality:fixtures.bzl", "real_source_target")

package(default_visibility = ["//:__subpackages__"])

# gazelle:resolve kotlin ImmutableList @maven//:com_google_guava_guava

# Handwritten: the generator never emits kotlin_test; test-owned `*Test.kt`
# stays out of the generated library and this rule survives regeneration.
kotlin_test(
    name = "greet_test",
    srcs = ["GreetTest.kt"],
    test_class = "greet.GreetTest",
    deps = [
        ":greet",
        "@maven//:junit_junit",
    ],
)

kotlin_library(
    name = "greet",
    srcs = [
        "Greet.kt",
        "Guava.kt",
        "Helper.kt",
    ],
    deps = ["@maven//:com_google_guava_guava"],
)

real_source_target(
    name = "corpus_starlark",
    starlark_srcs = ["BUILD.bazel"],
    tags = ["corpus"],
)
