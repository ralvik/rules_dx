load("@rules_dx//go/rules:defs.bzl", "go_library", "go_test")
load("//quality:fixtures.bzl", "real_source_target")

go_library(
    name = "greet",
    srcs = [
        "diff.go",
        "greet.go",
        "greet_linux.go",
        "greet_other.go",
        "helper.go",
    ],
    importpath = "example.com/adopt-go/greet",
    deps = ["@com_github_google_go_cmp//cmp"],
)

go_test(
    name = "greet_test",
    srcs = [
        "diff_test.go",
        "external_test.go",
        "greet_test.go",
        "helper_test.go",
    ],
    embed = [":greet"],
)

real_source_target(
    name = "corpus_starlark",
    aspect_hints = ["//:buildifier_config"],
    starlark_srcs = ["BUILD.bazel"],
    tags = ["corpus"],
)
