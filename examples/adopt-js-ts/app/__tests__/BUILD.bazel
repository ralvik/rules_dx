load("@rules_dx//javascript/rules:defs.bzl", "javascript_library")
load("//quality:fixtures.bzl", "real_source_target")

javascript_library(
    name = "data",
    srcs = ["data.js"],
)

real_source_target(
    name = "corpus_starlark",
    aspect_hints = ["//:buildifier_config"],
    starlark_srcs = ["BUILD.bazel"],
    tags = ["corpus"],
)
