// Package astro implements the first-party Astro Gazelle extension
// (O41, M20). It is implemented directly against Gazelle and the
// selected public JavaScript ruleset APIs over the pinned Astro
// component boundary. There is no generic container parser
// or shared helper layer: helpers are extracted only after a later
// framework implementation proves concrete reuse.
package astro
