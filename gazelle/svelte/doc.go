// Package svelte implements the first-party Svelte Gazelle extension
//. It is implemented directly against Gazelle and the
// selected public JavaScript ruleset APIs over the pinned Svelte
// component boundary. There is no generic container parser
// or shared helper layer: helpers are extracted only after a later
// framework implementation proves concrete reuse.
package svelte
