[package]
name = "dx_digest"
version = "0.0.0"
edition = "2021"

[lib]
name = "dx_digest"
path = "src/lib.rs"

[dependencies]
blake3 = "1"
hex = "0.4"
sha2 = "0.10"
thiserror = "2"

[dev-dependencies]
# Property pilot (issue #225): hex/digest round-trips as proptest
# properties alongside the fixed official vectors.
proptest = "1"
