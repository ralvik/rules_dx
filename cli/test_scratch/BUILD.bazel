"""Test-only scratch directories for dx crate tests (#237)."""

load("//rust/rules:defs.bzl", "dx_rust_crate")

package(default_visibility = [
    "//cli:__pkg__",
    "//cli:__subpackages__",
])

# M02 leaf-crate pattern via `dx_rust_crate` (issue #239).
dx_rust_crate(
    name = "dx_test_scratch",
    package_name = "cli/dx_test_scratch",
    deps = ["tempfile"],
)
