[package]
name = "dx_setup"
version = "0.0.0"
edition = "2021"

[lib]
name = "dx_setup"
path = "src/lib.rs"

[dependencies]
thiserror = "2"

[dev-dependencies]
# Golden pilot (issue #225): setup fingerprint golden as an insta snapshot.
insta = "1"
