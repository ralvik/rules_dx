Strict clap parsing (issue #810) plus help verb redirect (issue #951)
unknown options rejected with whole-token echo (no silent ignore)
missing values rejected naming the bare flag
hyphen-led tokens never consumed as option values
bad output/fail-on/report/min-coverage rejected with contract shapes
typo suggestions come from the same grammar
known flags on wrong commands rejected as unsupported
dx bazel tails forward verbatim with prefix options rejected
help verb redirect with help flags (dx help [command] matches --help)
generated help from the same grammar for all 32 commands
prefix inference rejected
hyphen-value consumption rejected
silent ignore rejected
qualified seed-only under issue #810
no Supported claim
