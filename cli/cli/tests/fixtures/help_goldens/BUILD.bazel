
load("//quality:fixtures.bzl", "real_source_target")

package(
    default_testonly = True,
    default_visibility = ["//:__subpackages__"],
)

exports_files(
    [
        "pins.bzl",
        "help_goldens.expected",
        "top_help.golden",
        "lint_help.golden",
        "build_help.golden",
        "clean_help.golden",
        "bazel_help.golden",
        "docs_help.golden",
    ],
)

real_source_target(
    name = "corpus_starlark",
    testonly = True,
    aspect_hints = ["//:buildifier_config"],
    starlark_srcs = [
        "BUILD.bazel",
        "pins.bzl",
    ],
    tags = ["corpus"],
)
