Correlation plus committed-change manifest (issue #811)
schema 1.1 with correlation plus manifest forward-compatible
run operations carry run:<target> in execution order
update per-set reports carry update:<set> with file pairs sharing it
manifest validates owning set plus declared file locks in path order
modify carries digest with pre-commit length as 0..old_len span
create omits digest with zero length as 0..0 insertion
directory hub third_party/dotnet/deps never validates as file change
empty or absent manifests emit nothing preserving v1.0 per-set contract
v1.0 consumers ignore correlation and omitted correlation preserves v1.0
1.0 witnesses decode under 1.1 by major-only enforcement
line order authoritative and correlation advisory grouping only
git scan rejected
BUILD parse rejected
rerun rejected
qualified seed-only under issue #811
no Supported claim
