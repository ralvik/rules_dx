"""Rust implementation root for the `dx` CLI.

No Bazel API lives here: `//dx` (`dx/BUILD.bazel`) is the Starlark-only
consumer facade, and every `cli/*` library serves that implementation.
Only `//cli/cli:dx` and `//cli/env:env` are public tool entry points.
"""

load("//quality:fixtures.bzl", "real_source_target")

package(default_visibility = ["//:__subpackages__"])

real_source_target(
    name = "corpus_starlark",
    aspect_hints = ["//:buildifier_config"],
    starlark_srcs = ["BUILD.bazel"],
    tags = ["corpus"],
)
