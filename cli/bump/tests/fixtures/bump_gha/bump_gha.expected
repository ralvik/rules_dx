Bump GHA tag-to-SHA auto resolution (issue #640)
actions/checkout v5 auto-resolves to SHA via GitHub releases
actions/cache v4 auto-resolves to SHA via GitHub releases
tag via version::parse GitTag then SHA via version::parse GitCommit
resolve_tag matches owner/repo plus tag (one tag per run)
unknown tag fails closed (nothing widened; never invent a SHA)
direct tag needs SHA resolution via upstream client before file edit
manual SHA only rejected
custom HTTP rejected
invented SHA rejected
private resolver rejected
one tag per run (never batch)
qualified seed-only under issue #640
no Supported claim
