Bump then update chaining (issue #638)
cargo widen then automatic CARGO_BAZEL_REPIN=1 repin
npm widen then automatic pnpm selective refresh
go widen then automatic noop refresh (no launch)
maven widen then automatic REPIN=1 pin
nuget widen then automatic paket2bazel regen
bazel/gha file-only (no refresh launch)
cargo:anyhow refreshes cargo
npm:jest refreshes npm:jest
maven:junit:junit refreshes maven
nuget:FSharp.Core refreshes nuget
widen kept in manifest on refresh failure (update_failed, no rollback)
major bump 1.2.3 -> 2.0.0 via migrate-v1-to-v2.json (migrate_failed exit 1; missing-versions exit 2)
manual second step rejected
private resolver rejected
qualified seed-only under issue #638
no Supported claim
