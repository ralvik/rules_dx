[package]
name = "dx_apply"
version = "0.0.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[lib]
name = "dx_apply"
path = "src/lib.rs"
