"""WP1 process boundary for the `dx` CLI."""

load("@rules_dx//quality:fixtures.bzl", "real_source_target")
load("//rust/rules:defs.bzl", "dx_rust_crate")
load("//tools/visibility:visibility.bzl", "VIS_CLI_DEPLOY")

package(default_visibility = VIS_CLI_DEPLOY)

dx_rust_crate(
    name = "dx_process",
    package_name = "cli/dx_process",
    srcs = [
        "src/lib.rs",
        "src/lib_tests.rs",
    ],
    extra_test_deps = ["//cli/test_scratch:dx_test_scratch"],
    deps = [
        "libc",
        "thiserror",
    ],
)

real_source_target(
    name = "corpus_starlark",
    aspect_hints = ["//:buildifier_config"],
    starlark_srcs = ["BUILD.bazel"],
    tags = ["corpus"],
)

real_source_target(
    name = "corpus_toml",
    tags = ["corpus"],
    toml_srcs = ["Cargo.toml"],
)
