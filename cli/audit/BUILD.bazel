load("@rules_dx//quality:fixtures.bzl", "real_source_target")

"""M26 WP1 slice 1: pure `dx audit` request planning (O11/O58)."""

load("//rust/rules:defs.bzl", "dx_rust_crate")

package(default_visibility = [
    "//cli:__pkg__",
    "//cli:__subpackages__",
])

# M02 leaf-crate pattern via `dx_rust_crate` (issue #239): emits
# `dx_audit` + `dx_audit_test` + fmt/clippy tests +
# `Cargo.toml` export + `corpus`, identical to the hand-written stanzas.
dx_rust_crate(
    name = "dx_audit",
    package_name = "cli/dx_audit",
    srcs = [
        "src/advisory.rs",
        "src/backend.rs",
        "src/exception.rs",
        "src/lib.rs",
        "src/license_expr.rs",
        "src/license_notice.rs",
        "src/license_policy.rs",
        "src/locks.rs",
        "src/outcome.rs",
        "src/secrets.rs",
        "src/spdx.rs",
        "src/vuln.rs",
    ],
    extra_deps = ["//cli/digest:dx_digest"],
    deps = [
        "chrono",
        "packageurl",
        "regex",
        "semver",
        "serde",
        "serde-sarif",
        "serde_json",
        "spdx",
        "thiserror",
        "toml",
        "url",
        "yaml_serde",
    ],
)

real_source_target(
    name = "corpus_starlark",
    starlark_srcs = ["BUILD.bazel"],
    tags = ["corpus"],
)

real_source_target(
    name = "corpus_toml",
    tags = ["corpus"],
    toml_srcs = ["Cargo.toml"],
)
