"""Provisional PowerShell Gallery lock authority (ADR 0032)."""

package(default_visibility = ["//visibility:public"])

exports_files([
    "PSGallery.lock.json",
    "PSGallery.requirements.psd1",
])
