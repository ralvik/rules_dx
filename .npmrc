# Shared pnpm config for the Node single-importer workspaces (see
# docs/tools/tool-acquisition.md). rules_js recommends
# hoist=false so host pnpm lays out node_modules like the Bazel-managed
# tree (ADR 0013).
hoist=false
