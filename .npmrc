# Single shared pnpm config for both JS workspaces (root `npm` hub and
# `quality/tools/javascript` `npm_tools` hub). rules_js recommends
# hoist=false so host pnpm lays out node_modules like the Bazel-managed
# tree (ADR 0013); see docs/tools/tool-acquisition.md.
hoist=false
