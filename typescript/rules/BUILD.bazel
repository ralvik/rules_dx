"""Experimental minimal TypeScript wrappers (M16, ADR 0013)."""

load("//quality:fixtures.bzl", "real_source_target")
load(":wrapper_tests.bzl", "typescript_wrapper_contract_tests")

package(default_visibility = ["//visibility:public"])

exports_files([
    "defs.bzl",
    "wrapper_tests.bzl",
])

# Repository corpus: wrapper Starlark owned for direct-Bazel
# dogfood under the canonical real_fixture_policy.
# Wrapper-contract negatives (#87 item 2): tsc-pipeline-only enforcement.
typescript_wrapper_contract_tests(name = "wrapper_contract_tests")

real_source_target(
    name = "corpus_starlark",
    starlark_srcs = [
        "BUILD.bazel",
        "defs.bzl",
        "wrapper_tests.bzl",
    ],
    tags = ["corpus"],
)
