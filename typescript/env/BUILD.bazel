"""Focused TypeScript environment plans (M16 WP3).

Each `typescript_env_plan` reads the analyzed authoritative providers of one
`dx_ts_project` wrapper: the preserved `JsInfo` and `TsConfigInfo` plus
`QualitySourcesInfo`. Focused-target only; repository/root/exact-target
orchestration remains M25.
"""

load("//quality:fixtures.bzl", "real_source_target")
load("//typescript/env:plan.bzl", "typescript_env_plan")
load("//typescript/env:plan_tests.bzl", "env_plan_tests")

package(default_visibility = ["//visibility:public"])

typescript_env_plan(
    name = "hello_lib_plan",
    target = "//typescript/hello:hello_lib",
)

env_plan_tests(
    name = "env_plan_tests",
    subjects = [
        ":hello_lib_plan",
    ],
)

# Repository corpus (M05 WP1): plan Starlark owned for direct-Bazel dogfood
# under the canonical real_fixture_policy.
real_source_target(
    name = "corpus",
    starlark_srcs = [
        "BUILD.bazel",
        "plan.bzl",
        "plan_tests.bzl",
    ],
)
