"""M16 thin entry fixture: library owns main.ts (execution reuses the JS wrappers over compiled outputs)."""

load("//quality:fixtures.bzl", "real_source_target")
load("//typescript/rules:defs.bzl", "typescript_project")

package(
    default_visibility = ["//:__subpackages__"],
    default_testonly = True,
)

typescript_project(
    name = "helper",
    srcs = ["helper.ts"],
    declaration = True,
    transpiler = "tsc",
    tsconfig = "tsconfig.json",
)

typescript_project(
    name = "main",
    srcs = ["main.ts"],
    declaration = True,
    transpiler = "tsc",
    tsconfig = "tsconfig.json",
    deps = [":helper"],
)

real_source_target(
    name = "corpus_starlark",
    starlark_srcs = ["BUILD.bazel"],
    tags = ["corpus"],
)
