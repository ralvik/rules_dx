Roots plus BEP plus cold-warm evidence (issue #506).

Frozen strategy: FROZEN_STRATEGY is RecursivePattern on the //...
baseline with FROZEN_EVIDENCE plus INCREMENTALITY_EVIDENCE, exact-target
bypass, and declared-BUILD-only behavior by fiat per ADR 0022.

BEP: one BEP stream carries the private dx_env_plans plus dx_codegen_plans
output groups with .dxenv.pb plus .dxcodegen.pb shard suffixes; the CLI
never implements a second remote-cache downloader and never scans
bazel-out.

Cold-warm: cold_ms + WARM_WEIGHT with WARM_WEIGHT = 2, baseline wins
ties, and no timing claims are made per [ADR 0022]; cold proportional to
the selected graph, warm via the persistent server plus action cache by
design.

Provenance: cli/roots roots_pattern_fixture plus roots_monolith_fixture
with repository_roots_file plus roots_aggregate wiring; generation plus
env shard-emission fixtures with transitive aspect collection.

Admitted-pairs evolution onboarding (issue #788): admitted-pairs evolution
checklist with narrow ruleset-specific adapter plus dx_codegen_plans plus
.dxcodegen.pb plus symlink-only read-only mirror plus FROZEN_STRATEGY plus
cold_ms + WARM_WEIGHT; protobuf/rust via
//generation:codegen_prost_fixture plus //generation:codegen_shard_alpha
plus //generation:codegen_shard_beta with
//generation:codegen_plan_chain_subject plus
//generation:codegen_plan_prost_subject.

Platform plus consumer plus release linkage (issue #787, beyond
seed-host-delivered): platform hosts linux_x86_64 plus linux_arm64
plus macos_arm64 plus windows_x86_64 with symlink-only on every required
host plus unsupported_platform refusal plus per-cell coverage with no
cross-cell union; consumer via examples/adopt-rust plus
examples/adopt-python plus examples/adopt-js-ts plus
.github/workflows/reusable-consumer.yml plus
examples/consumer-ci/caller.yml; release via //deploy/release:sbom_demo
plus //deploy/release:signing_demo plus //deploy/release:bcr_demo plus
//deploy/release:release_driver plus //deploy/install:dx_verify plus
docs/product/promotion-checklist.md plus
bazel run //tools/ci:supported_evidence_gate; bare-schema expansion
stays open under #751 plus collision replacement contract stays open
under #752 plus concurrency plus NFS plus relock stays open under #753;
no Supported claim.
