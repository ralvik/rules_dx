# CI policy notes live in `.github/workflows/README.md` (split from header comments, no behavior change).

name: ci

on:
  push:
    branches: [main]
  pull_request:

env:
  # Preserve Bazel labels passed through Git Bash to native Windows binaries.
  MSYS_NO_PATHCONV: "1"
  MSYS2_ARG_CONV_EXCL: "*"

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  docs-ci:
    name: docs-ci (self-call reusable docs workflow)
    uses: ./.github/workflows/reusable-docs.yml
    with:
      rules_dx_version: "0.0.0"
      docs_scope: "//docs/..."
      docs_dir: "docs"
      publish: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
    permissions:
      contents: read
      pages: write
      id-token: write
  dogfood:
    name: dogfood (self-call reusable consumer workflow)
    # Dogfood-like consumer (issue #408, Phase 1 issue #607): proves the
    # reusable file parses, the pin check passes, and eight checks execute
    # end-to-end on the working tree (local path: no version-pin
    # chicken-and-egg; rules_dx_version matches MODULE.bazel). `test`
    # stays disabled: `dx coverage` resolves scope via `resolve_for_test`
    # (same as `dx test`, `cli/cli/src/exec/workflow.rs`) and runs `bazel
    # coverage`, which executes the tests, so running both doubles test
    # execution. Verbatim `//...` scope, same entrypoints as external
    # consumers. Runs on all qualified hosts (seed plus arm64 native plus
    # macOS arm64 plus Windows x86_64
    # MSVC-compatible, issues #410/#412/#414).
    uses: ./.github/workflows/reusable-consumer.yml
    with:
      rules_dx_version: "0.0.0"
      platforms: '["linux_x86_64", "linux_arm64", "macos_arm64", "windows_x86_64"]'
      disabled_checks: "test"
    permissions:
      contents: read
      checks: write
      pull-requests: write
  build:
    name: build (bazel build //...)
    timeout-minutes: 30
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-seed-
      - name: Build workspace
        run: bazel build --noshow_progress //...
      - name: Adopt-rust dx_dev smoke (local_path_override + dx_dev wiring, issue #407)
        run: bazel build --noshow_progress //examples/adopt-rust/... --config=dx_dev
      - name: Write job summary
        if: always()
        run: |
          echo "### build — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Command: bazel build //... plus adopt-rust dx_dev smoke (seed host, local-only; hermetic CLI-contract, issue #407)." >> "$GITHUB_STEP_SUMMARY"
  sbom:
    name: sbom (SBOM + provenance build/verify/upload, seed host)
    # SBOM plus provenance on CI (issue #612): sbom_demo only ran on
    # workflow_dispatch dry-run, never built or verified on push, with no
    # attestation. This job builds //deploy/release:sbom_demo plus verifies
    # via //deploy/release:dx_release_tools_test on every push/PR (seed host),
    # stages SPDX-2.3 + SLSA v1 under RUNNER_TEMP with a digests plus origin
    # bind, and uploads as a CI artifact for inspection. Push-to-main runs
    # additionally attest the staged pair via actions/attest (Sigstore,
    # fork-safe: PRs never attest, publishes nothing). Real release signing
    # stays owner-gated human-run via //deploy/release:signing_demo (fork-safe:
    # CI never signs PR code, publishes nothing).
    needs: [build]
    timeout-minutes: 30
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
      attestations: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-seed-
      - name: Build SBOM + provenance demo
        run: bazel build --noshow_progress //deploy/release:sbom_demo
      - name: Verify SBOM + provenance bind artifact
        run: bazel test --noshow_progress --flaky_test_attempts=3 --test_timeout=300 //deploy/release:dx_release_tools_test
      - name: Stage SBOM + provenance for upload
        run: |
          set -euo pipefail
          stage="$RUNNER_TEMP/sbom"
          rm -rf "$stage"
          mkdir -p "$stage"
          spdx="$(bazel cquery --output=files //deploy/release:sbom_demo_spdx)"
          prov="$(bazel cquery --output=files //deploy/release:sbom_demo_provenance)"
          cp "$spdx" "$stage/sbom_demo.spdx.json"
          cp "$prov" "$stage/sbom_demo.provenance.json"
          grep -q -F '"spdxVersion": "SPDX-2.3"' "$stage/sbom_demo.spdx.json"
          grep -q -F '"predicateType": "https://slsa.dev/provenance/v1"' "$stage/sbom_demo.provenance.json"
          grep -q -F '"_type": "https://in-toto.io/Statement/v1"' "$stage/sbom_demo.provenance.json"
          printf '%s %s %s %s\n' "${GITHUB_REPOSITORY}" "${GITHUB_SHA}" "${GITHUB_RUN_ID}" "${GITHUB_REF}" > "$stage/origin.txt"
          cat "$stage/origin.txt"
          python3 --version
          python3 -c 'import sys; assert sys.version_info >= (3, 9), sys.version'
          python3 - "$stage" <<'EOF'
          import hashlib
          import pathlib
          import sys
          stage = pathlib.Path(sys.argv[1])
          lines = []
          for name in ("sbom_demo.spdx.json", "sbom_demo.provenance.json"):
              digest = hashlib.sha256((stage / name).read_bytes()).hexdigest()
              lines.append(f"{digest}  {name}\n")
              print(f"{name}: {digest}")
          (stage / "digests.txt").write_text("".join(lines), encoding="utf-8")
          EOF
          cat "$stage/digests.txt"
      - name: Upload SBOM + provenance artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: sbom-provenance
          path: ${{ runner.temp }}/sbom
          if-no-files-found: error
          retention-days: 7
      - name: Attest SBOM + provenance (push to main only, fork-safe)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4.2.2
        with:
          subject-path: '${{ runner.temp }}/sbom/*.json'
          show-summary: true
      - name: Write job summary
        if: always()
        run: |
          echo "### sbom — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Commands: bazel build //deploy/release:sbom_demo; bazel test //deploy/release:dx_release_tools_test; stage SPDX-2.3 + SLSA v1 under RUNNER_TEMP/sbom with digests plus origin bind and upload as sbom-provenance (seed host, publishes nothing; push-to-main additionally attests via actions/attest, PRs never attest; release signing stays owner-gated human-run via //deploy/release:signing_demo, issue #612)." >> "$GITHUB_STEP_SUMMARY"
  sbom-arm64:
    name: sbom-arm64 (SBOM + provenance build/verify/upload, linux arm64)
    # Per-host release evidence for Linux arm64 glibc (issue #803):
    # mirrors the seed sbom job on the arm64 native runner
    # (ubuntu-24.04-arm, bazel-arm64- cache scope, local-only). Builds
    # //deploy/release:sbom_demo plus verifies via
    # //deploy/release:dx_release_tools_test on every push/PR, stages
    # SPDX-2.3 + SLSA v1 under RUNNER_TEMP, and uploads as a per-host CI
    # artifact for inspection. Real Sigstore plus GitHub attestation stays
    # owner-gated human-run via //deploy/release:signing_demo (fork-safe:
    # CI never signs PR code, publishes nothing).
    needs: [build-arm64]
    timeout-minutes: 30
    runs-on: ubuntu-24.04-arm
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-arm64-
      - name: Build SBOM + provenance demo
        run: bazel build --noshow_progress //deploy/release:sbom_demo
      - name: Verify SBOM + provenance bind artifact
        run: bazel test --noshow_progress --flaky_test_attempts=3 --test_timeout=300 //deploy/release:dx_release_tools_test
      - name: Stage SBOM + provenance for upload
        run: |
          set -euo pipefail
          stage="$RUNNER_TEMP/sbom-arm64"
          rm -rf "$stage"
          mkdir -p "$stage"
          spdx="$(bazel cquery --output=files //deploy/release:sbom_demo_spdx)"
          prov="$(bazel cquery --output=files //deploy/release:sbom_demo_provenance)"
          cp "$spdx" "$stage/sbom_demo.spdx.json"
          cp "$prov" "$stage/sbom_demo.provenance.json"
          grep -q -F '"spdxVersion": "SPDX-2.3"' "$stage/sbom_demo.spdx.json"
          grep -q -F '"predicateType": "https://slsa.dev/provenance/v1"' "$stage/sbom_demo.provenance.json"
          grep -q -F '"_type": "https://in-toto.io/Statement/v1"' "$stage/sbom_demo.provenance.json"
          python3 --version
          python3 -c 'import sys; assert sys.version_info >= (3, 9), sys.version'
          python3 - "$stage" <<'EOF'
          import hashlib
          import pathlib
          import sys
          stage = pathlib.Path(sys.argv[1])
          lines = []
          for name in ("sbom_demo.spdx.json", "sbom_demo.provenance.json"):
              digest = hashlib.sha256((stage / name).read_bytes()).hexdigest()
              lines.append(f"{digest}  {name}\n")
              print(f"{name}: {digest}")
          (stage / "digests.txt").write_text("".join(lines), encoding="utf-8")
          EOF
          cat "$stage/digests.txt"
      - name: Upload SBOM + provenance artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: sbom-provenance-linux_arm64
          path: ${{ runner.temp }}/sbom-arm64
          if-no-files-found: error
          retention-days: 7
      - name: Write job summary
        if: always()
        run: |
          echo "### sbom-arm64 — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Commands: bazel build //deploy/release:sbom_demo; bazel test //deploy/release:dx_release_tools_test; stage SPDX-2.3 + SLSA v1 under RUNNER_TEMP/sbom-arm64 and upload as sbom-provenance-linux_arm64 (linux arm64 native, local-only, publishes nothing; attestation stays owner-gated human-run via //deploy/release:signing_demo, issue #803)." >> "$GITHUB_STEP_SUMMARY"
  sbom-musl-x86_64:
    name: sbom-musl-x86_64 (SBOM + provenance build/verify/upload, linux x86_64 static musl)
    # Per-profile release evidence for Linux x86_64 static musl (issue #804):
    # mirrors the seed sbom job on the musl x86_64 runner
    # (ubuntu-latest, bazel-musl-x86_64- cache scope, local-only). Builds
    # //deploy/release:sbom_demo plus verifies via
    # //deploy/release:dx_release_tools_test on every push/PR, stages
    # SPDX-2.3 + SLSA v1 under RUNNER_TEMP, and uploads as a per-profile CI
    # artifact for inspection. Static native closure only; dynamic musl
    # explicitly out of scope. Real Sigstore plus GitHub attestation stays
    # owner-gated human-run via //deploy/release:signing_demo (fork-safe:
    # CI never signs PR code, publishes nothing).
    needs: [build-musl-x86_64]
    timeout-minutes: 30
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-musl-x86_64-
      - name: Build SBOM + provenance demo
        run: bazel build --noshow_progress //deploy/release:sbom_demo
      - name: Verify SBOM + provenance bind artifact
        run: bazel test --noshow_progress --flaky_test_attempts=3 --test_timeout=300 //deploy/release:dx_release_tools_test
      - name: Stage SBOM + provenance for upload
        run: |
          set -euo pipefail
          stage="$RUNNER_TEMP/sbom-musl-x86_64"
          rm -rf "$stage"
          mkdir -p "$stage"
          spdx="$(bazel cquery --output=files //deploy/release:sbom_demo_spdx)"
          prov="$(bazel cquery --output=files //deploy/release:sbom_demo_provenance)"
          cp "$spdx" "$stage/sbom_demo.spdx.json"
          cp "$prov" "$stage/sbom_demo.provenance.json"
          grep -q -F '"spdxVersion": "SPDX-2.3"' "$stage/sbom_demo.spdx.json"
          grep -q -F '"predicateType": "https://slsa.dev/provenance/v1"' "$stage/sbom_demo.provenance.json"
          grep -q -F '"_type": "https://in-toto.io/Statement/v1"' "$stage/sbom_demo.provenance.json"
          python3 --version
          python3 -c 'import sys; assert sys.version_info >= (3, 9), sys.version'
          python3 - "$stage" <<'EOF'
          import hashlib
          import pathlib
          import sys
          stage = pathlib.Path(sys.argv[1])
          lines = []
          for name in ("sbom_demo.spdx.json", "sbom_demo.provenance.json"):
              digest = hashlib.sha256((stage / name).read_bytes()).hexdigest()
              lines.append(f"{digest}  {name}\n")
              print(f"{name}: {digest}")
          (stage / "digests.txt").write_text("".join(lines), encoding="utf-8")
          EOF
          cat "$stage/digests.txt"
      - name: Upload SBOM + provenance artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: sbom-provenance-linux_x86_64_musl
          path: ${{ runner.temp }}/sbom-musl-x86_64
          if-no-files-found: error
          retention-days: 7
      - name: Write job summary
        if: always()
        run: |
          echo "### sbom-musl-x86_64 — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Commands: bazel build //deploy/release:sbom_demo; bazel test //deploy/release:dx_release_tools_test; stage SPDX-2.3 + SLSA v1 under RUNNER_TEMP/sbom-musl-x86_64 and upload as sbom-provenance-linux_x86_64_musl (linux x86_64 static musl, local-only, static closure only with dynamic musl explicitly out of scope, publishes nothing; attestation stays owner-gated human-run via //deploy/release:signing_demo, issue #804)." >> "$GITHUB_STEP_SUMMARY"
  sbom-musl-arm64:
    name: sbom-musl-arm64 (SBOM + provenance build/verify/upload, linux arm64 static musl)
    # Per-profile release evidence for Linux arm64 static musl (issue #804):
    # mirrors the seed sbom job on the musl arm64 runner
    # (ubuntu-24.04-arm, bazel-musl-arm64- cache scope, local-only). Builds
    # //deploy/release:sbom_demo plus verifies via
    # //deploy/release:dx_release_tools_test on every push/PR, stages
    # SPDX-2.3 + SLSA v1 under RUNNER_TEMP, and uploads as a per-profile CI
    # artifact for inspection. Static native closure only; dynamic musl
    # explicitly out of scope. Real Sigstore plus GitHub attestation stays
    # owner-gated human-run via //deploy/release:signing_demo (fork-safe:
    # CI never signs PR code, publishes nothing).
    needs: [build-musl-arm64]
    timeout-minutes: 30
    runs-on: ubuntu-24.04-arm
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-musl-arm64-
      - name: Build SBOM + provenance demo
        run: bazel build --noshow_progress //deploy/release:sbom_demo
      - name: Verify SBOM + provenance bind artifact
        run: bazel test --noshow_progress --flaky_test_attempts=3 --test_timeout=300 //deploy/release:dx_release_tools_test
      - name: Stage SBOM + provenance for upload
        run: |
          set -euo pipefail
          stage="$RUNNER_TEMP/sbom-musl-arm64"
          rm -rf "$stage"
          mkdir -p "$stage"
          spdx="$(bazel cquery --output=files //deploy/release:sbom_demo_spdx)"
          prov="$(bazel cquery --output=files //deploy/release:sbom_demo_provenance)"
          cp "$spdx" "$stage/sbom_demo.spdx.json"
          cp "$prov" "$stage/sbom_demo.provenance.json"
          grep -q -F '"spdxVersion": "SPDX-2.3"' "$stage/sbom_demo.spdx.json"
          grep -q -F '"predicateType": "https://slsa.dev/provenance/v1"' "$stage/sbom_demo.provenance.json"
          grep -q -F '"_type": "https://in-toto.io/Statement/v1"' "$stage/sbom_demo.provenance.json"
          python3 --version
          python3 -c 'import sys; assert sys.version_info >= (3, 9), sys.version'
          python3 - "$stage" <<'EOF'
          import hashlib
          import pathlib
          import sys
          stage = pathlib.Path(sys.argv[1])
          lines = []
          for name in ("sbom_demo.spdx.json", "sbom_demo.provenance.json"):
              digest = hashlib.sha256((stage / name).read_bytes()).hexdigest()
              lines.append(f"{digest}  {name}\n")
              print(f"{name}: {digest}")
          (stage / "digests.txt").write_text("".join(lines), encoding="utf-8")
          EOF
          cat "$stage/digests.txt"
      - name: Upload SBOM + provenance artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: sbom-provenance-linux_arm64_musl
          path: ${{ runner.temp }}/sbom-musl-arm64
          if-no-files-found: error
          retention-days: 7
      - name: Write job summary
        if: always()
        run: |
          echo "### sbom-musl-arm64 — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Commands: bazel build //deploy/release:sbom_demo; bazel test //deploy/release:dx_release_tools_test; stage SPDX-2.3 + SLSA v1 under RUNNER_TEMP/sbom-musl-arm64 and upload as sbom-provenance-linux_arm64_musl (linux arm64 static musl, local-only, static closure only with dynamic musl explicitly out of scope, publishes nothing; attestation stays owner-gated human-run via //deploy/release:signing_demo, issue #804)." >> "$GITHUB_STEP_SUMMARY"
  sbom-macos-arm64:
    name: sbom-macos-arm64 (SBOM + provenance build/verify/upload, macos arm64)
    # Per-host release evidence for macOS arm64 native (issue #805):
    # mirrors the seed sbom job on the macos arm64 native runner
    # (macos-14, bazel-macos-arm64- cache scope, local-only). Builds
    # //deploy/release:sbom_demo plus verifies via
    # //deploy/release:dx_release_tools_test on every push/PR, stages
    # SPDX-2.3 + SLSA v1 under RUNNER_TEMP, and uploads as a per-host CI
    # artifact for inspection. Pinned acquired SDK with the hermetic-llvm
    # Apple-SDK backend provisional and no host-installed SDK fallback.
    # Real Sigstore plus GitHub attestation stays owner-gated human-run
    # via //deploy/release:signing_demo (fork-safe: CI never signs PR
    # code, publishes nothing).
    needs: [build-macos-arm64]
    timeout-minutes: 60
    runs-on: macos-14
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-macos-arm64-
      - name: Build SBOM + provenance demo
        run: bazel build --noshow_progress //deploy/release:sbom_demo
      - name: Verify SBOM + provenance bind artifact
        run: bazel test --noshow_progress --flaky_test_attempts=3 --test_timeout=300 //deploy/release:dx_release_tools_test
      - name: Stage SBOM + provenance for upload
        run: |
          set -euo pipefail
          stage="$RUNNER_TEMP/sbom-macos-arm64"
          rm -rf "$stage"
          mkdir -p "$stage"
          spdx="$(bazel cquery --output=files //deploy/release:sbom_demo_spdx)"
          prov="$(bazel cquery --output=files //deploy/release:sbom_demo_provenance)"
          cp "$spdx" "$stage/sbom_demo.spdx.json"
          cp "$prov" "$stage/sbom_demo.provenance.json"
          grep -q -F '"spdxVersion": "SPDX-2.3"' "$stage/sbom_demo.spdx.json"
          grep -q -F '"predicateType": "https://slsa.dev/provenance/v1"' "$stage/sbom_demo.provenance.json"
          grep -q -F '"_type": "https://in-toto.io/Statement/v1"' "$stage/sbom_demo.provenance.json"
          python3 --version
          python3 -c 'import sys; assert sys.version_info >= (3, 9), sys.version'
          python3 - "$stage" <<'EOF'
          import hashlib
          import pathlib
          import sys
          stage = pathlib.Path(sys.argv[1])
          lines = []
          for name in ("sbom_demo.spdx.json", "sbom_demo.provenance.json"):
              digest = hashlib.sha256((stage / name).read_bytes()).hexdigest()
              lines.append(f"{digest}  {name}\n")
              print(f"{name}: {digest}")
          (stage / "digests.txt").write_text("".join(lines), encoding="utf-8")
          EOF
          cat "$stage/digests.txt"
      - name: Upload SBOM + provenance artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: sbom-provenance-macos_arm64
          path: ${{ runner.temp }}/sbom-macos-arm64
          if-no-files-found: error
          retention-days: 7
      - name: Write job summary
        if: always()
        run: |
          echo "### sbom-macos-arm64 — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Commands: bazel build //deploy/release:sbom_demo; bazel test //deploy/release:dx_release_tools_test; stage SPDX-2.3 + SLSA v1 under RUNNER_TEMP/sbom-macos-arm64 and upload as sbom-provenance-macos_arm64 (macos arm64 native, local-only, pinned acquired SDK with hermetic-llvm Apple-SDK backend provisional and no host-installed SDK fallback, publishes nothing; attestation stays owner-gated human-run via //deploy/release:signing_demo, issue #805)." >> "$GITHUB_STEP_SUMMARY"
  sbom-windows-x86_64:
    name: sbom-windows-x86_64 (SBOM + provenance build/verify/upload, windows x86_64)
    # Per-host release evidence for Windows x86_64 MSVC-compatible (issue #807):
    # mirrors the seed sbom job on the windows native runner
    # (windows-latest with shell bash, bazel-windows-x86_64- cache scope,
    # local-only). Builds //deploy/release:sbom_demo plus verifies via
    # //deploy/release:dx_release_tools_test on every push/PR, stages
    # SPDX-2.3 + SLSA v1 under RUNNER_TEMP, and uploads as a per-host CI
    # artifact for inspection. Hermetic acquisition plus MSVC compatibility
    # gates unchanged; explicit EULA acceptance required, never automatic;
    # installed Build Tools fallback never approved. Real Sigstore plus
    # GitHub attestation stays owner-gated human-run via
    # //deploy/release:signing_demo (fork-safe: CI never signs PR code,
    # publishes nothing).
    needs: [build-windows-x86_64]
    timeout-minutes: 60
    runs-on: windows-latest
    defaults:
      run:
        shell: bash
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-windows-x86_64-
      - name: Build SBOM + provenance demo
        run: bazel build --noshow_progress //deploy/release:sbom_demo
      - name: Verify SBOM + provenance bind artifact
        run: bazel test --noshow_progress --flaky_test_attempts=3 --test_timeout=300 //deploy/release:dx_release_tools_test
      - name: Stage SBOM + provenance for upload
        run: |
          set -euo pipefail
          stage="$RUNNER_TEMP/sbom-windows-x86_64"
          rm -rf "$stage"
          mkdir -p "$stage"
          spdx="$(bazel cquery --output=files //deploy/release:sbom_demo_spdx)"
          prov="$(bazel cquery --output=files //deploy/release:sbom_demo_provenance)"
          cp "$spdx" "$stage/sbom_demo.spdx.json"
          cp "$prov" "$stage/sbom_demo.provenance.json"
          grep -q -F '"spdxVersion": "SPDX-2.3"' "$stage/sbom_demo.spdx.json"
          grep -q -F '"predicateType": "https://slsa.dev/provenance/v1"' "$stage/sbom_demo.provenance.json"
          grep -q -F '"_type": "https://in-toto.io/Statement/v1"' "$stage/sbom_demo.provenance.json"
          python3 --version
          python3 -c 'import sys; assert sys.version_info >= (3, 9), sys.version'
          python3 - "$stage" <<'EOF'
          import hashlib
          import pathlib
          import sys
          stage = pathlib.Path(sys.argv[1])
          lines = []
          for name in ("sbom_demo.spdx.json", "sbom_demo.provenance.json"):
              digest = hashlib.sha256((stage / name).read_bytes()).hexdigest()
              lines.append(f"{digest}  {name}\n")
              print(f"{name}: {digest}")
          (stage / "digests.txt").write_text("".join(lines), encoding="utf-8")
          EOF
          cat "$stage/digests.txt"
      - name: Upload SBOM + provenance artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: sbom-provenance-windows_x86_64
          path: ${{ runner.temp }}/sbom-windows-x86_64
          if-no-files-found: error
          retention-days: 7
      - name: Write job summary
        if: always()
        run: |
          echo "### sbom-windows-x86_64 — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Commands: bazel build //deploy/release:sbom_demo; bazel test //deploy/release:dx_release_tools_test; stage SPDX-2.3 + SLSA v1 under RUNNER_TEMP/sbom-windows-x86_64 and upload as sbom-provenance-windows_x86_64 (windows x86_64 MSVC-compatible native on windows-latest with shell bash, local-only, explicit EULA never automatic with no installed fallback, publishes nothing; attestation stays owner-gated human-run via //deploy/release:signing_demo, issue #807)." >> "$GITHUB_STEP_SUMMARY"
  preset-stale:
    name: preset-stale (dx update --check + root locks, Linux once)
    # Consumer-CI stale cell (issue #332) plus root-lock freshness
    # (issue #1060): Linux-once preset plus lock gate. Review-required
    # with no auto-merge per issue #332
    # (`dx_ci::preset_bot_auto_merge_allowed()==false`,
    # `preset_regen_requires_review()==true`): a stale fragment fails
    # here and must be fixed by a reviewed `dx update` plus flag-diff.
    # Root locks fail here when stale (existence-only rejected per
    # #1060): MODULE.bazel.lock via `mod deps --lockfile_mode=error`,
    # cargo-bazel-lock.json via the crate_universe repin target without
    # CARGO_BAZEL_REPIN, pnpm-lock.yaml via the Bazel-pinned pnpm
    # `--frozen-lockfile` install (maven stays fail-closed via
    # fail_if_repin_required in MODULE.bazel).
    timeout-minutes: 15
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-seed-
      - name: Check preset freshness
        run: |
          bazel run --noshow_progress //tools/bazelrc:preset_update -- --verify-only
          bazel run --noshow_progress //cli/cli:dx -- update --check
      - name: Check root lock freshness (issue #1060)
        run: |
          bazel mod deps --lockfile_mode=error > /dev/null
          bazel build --noshow_progress //rust/tests/fixtures/hello:hello
          bazel run --noshow_progress @pnpm//:pnpm -- install --frozen-lockfile --lockfile-only
      - name: Write job summary
        if: always()
        run: |
          echo "### preset-stale — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Commands: bazel run //tools/bazelrc:preset_update -- --verify-only; bazel run //cli/cli:dx -- update --check; bazel mod deps --lockfile_mode=error; bazel build //rust/tests/fixtures/hello:hello (crate_universe check, no repin); bazel run @pnpm//:pnpm -- install --frozen-lockfile --lockfile-only (Linux once, review-required, no auto-merge)." >> "$GITHUB_STEP_SUMMARY"
  gitleaks:
    name: gitleaks (secret scan, Linux once)
    # Secret scanning (issue #1057): Linux-once `gitleaks detect --no-git`
    # over the working tree using the pinned vendored artifact
    # (@dx_gitleaks_linux_x86_64//:tool, v8.30.1, checksums in
    # quality/artifacts/gitleaks.*.bzl). Explicit `--config .gitleaks.toml`
    # pins the committed allowlist (scoped to docs prose via `paths:` plus
    # `regexTarget = "match"`); `--redact` plus `--exit-code 2` match the
    # `dx audit security` secrets invocation. `--no-git` treats the checkout
    # as a regular directory (no history scan).
    timeout-minutes: 15
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-seed-
      - name: Run gitleaks detect --no-git (pinned vendored artifact)
        run: |
          set -euo pipefail
          bazel build --noshow_progress @dx_gitleaks_linux_x86_64//:tool
          rel="$(bazel cquery --output=files @dx_gitleaks_linux_x86_64//:tool)"
          bin="$(bazel info execution_root)/$rel"
          "$bin" detect --source . --no-git --config .gitleaks.toml --redact --exit-code 2
      - name: Write job summary
        if: always()
        run: |
          echo "### gitleaks — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Command: bazel build @dx_gitleaks_linux_x86_64//:tool; gitleaks detect --source . --no-git --config .gitleaks.toml --redact --exit-code 2 (Linux once, pinned v8.30.1, allowlist scoped to docs prose, issue #1057)." >> "$GITHUB_STEP_SUMMARY"
  test:
    name: test (bazel test //...)
    needs: [build]
    timeout-minutes: 45
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-seed-
      - name: Test workspace
        run: bazel test --noshow_progress --flaky_test_attempts=3 --test_timeout=300 --local_test_jobs=4 //...
      - name: Report skip volume (Linux-only sh harness, issue #769)
        if: always()
        run: |
          echo "### skip volume — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Cell seed linux_x86_64 real run, 0 skips: all 58 sh_test executed, no Linux-only skip (budget pinned by bazel run //tools/ci:skip_budget_qualification, issue #769)." >> "$GITHUB_STEP_SUMMARY"
      - name: Write job summary
        if: always()
        run: |
          echo "### test — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Command: bazel test //... (seed host, local-only; manual targets excluded by Bazel tag semantics)." >> "$GITHUB_STEP_SUMMARY"
  coverage:
    name: coverage (dx coverage gate, seed cell)
    needs: [build]
    timeout-minutes: 45
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-seed-
      - name: Enforce seed coverage cell
        run: |
          set +e
          bazel run --noshow_progress //cli/cli:dx -- coverage --min-coverage 97 //... 2>&1 | tee "${RUNNER_TEMP:-/tmp}/dx_coverage.log"
          rc=${PIPESTATUS[0]}
          echo "$rc" > "${RUNNER_TEMP:-/tmp}/dx_coverage.rc"
          exit $rc
      - name: Render first-party coverage summary (seed cell, same LCOV)
        # First-party PR reporting (issue #254) plus the per-cell visibility
        # contract (issue #1062): Bazel-owned gate stays the source of truth;
        # this step only presents the verdict already computed by the gate.
        # Same coverage_bin gate plus same coverage_comment.sh shape as the
        # five non-seed cells (which stay step-summary only); only this seed
        # cell publishes the PR comment below, to avoid sixfold spam.
        # Execution runs without secrets so fork code never sees write
        # credentials; publication below uses GH_TOKEN only on non-fork PRs
        # and never executes fork targets.
        if: always()
        run: |
          set -euo pipefail
          export LLVM_PROFILE_FILE="${RUNNER_TEMP:-/tmp}/profraw_%m_%p.profraw"
          bazel build --noshow_progress //tools/coverage:coverage_bin //tools/coverage:coverage_comment >/dev/null 2>&1
          bazel coverage --noshow_progress //cli/lcov/... //tools/coverage/... //quality/evaluator/... \
            --combined_report=lcov \
            --instrumentation_filter='^//(cli/lcov|tools/coverage|quality/evaluator)' >/dev/null 2>&1 || true
          bazel query 'kind("source file", deps(//cli/lcov/... + //tools/coverage/... + //quality/evaluator/...))' 2>/dev/null \
            | grep -E '^//(cli/lcov|tools/coverage|quality/evaluator)[:/].*\.rs$' \
            | sed -e 's|^//||' -e 's|:|/|' \
            | LC_ALL=C sort -u > "${RUNNER_TEMP:-/tmp}/seed_sources.txt"
          set +e
          bazel-bin/tools/coverage/coverage_bin --report bazel-out/_coverage/_coverage_report.dat \
            --inventory tools/coverage/seed-inventory.txt --sources "${RUNNER_TEMP:-/tmp}/seed_sources.txt" --root . > "${RUNNER_TEMP:-/tmp}/seed_verdict.txt" 2>&1
          echo "$?" > "${RUNNER_TEMP:-/tmp}/seed_gate.rc"
          set -e
          dx_rc="$(cat "${RUNNER_TEMP:-/tmp}/dx_coverage.rc" 2>/dev/null || echo unknown)"
          cp "${RUNNER_TEMP:-/tmp}/dx_coverage.log" "${RUNNER_TEMP:-/tmp}/dx_coverage_out.txt" 2>/dev/null || true
          bazel run --noshow_progress //tools/coverage:coverage_comment -- \
            --cell "seed linux_x86_64" \
            --revision "${GITHUB_SHA}" \
            --run "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}" \
            --out "${RUNNER_TEMP:-/tmp}/coverage_comment.md" \
            --gate-output "${RUNNER_TEMP:-/tmp}/seed_verdict.txt" \
            --gate-exit "$(cat "${RUNNER_TEMP:-/tmp}/seed_gate.rc")" \
            --lcov bazel-out/_coverage/_coverage_report.dat \
            --dx-output "${RUNNER_TEMP:-/tmp}/dx_coverage_out.txt" \
            --dx-exit "$dx_rc"
          echo "### coverage summary (seed linux_x86_64, first-party, issue #254)" >> "$GITHUB_STEP_SUMMARY"
          cat "${RUNNER_TEMP:-/tmp}/coverage_comment.md" >> "$GITHUB_STEP_SUMMARY"
      - name: Publish PR summary comment (dedup marker, fork-safe)
        # One integration-owned updated comment per PR (docs/github-ci.md)
        # plus the per-cell visibility contract (issue #1062): only the seed
        # cell publishes; the five non-seed cells stay step-summary only by
        # design with the same rich shape. Dedup by marker; human comments
        # never touched; concurrency group cancels superseded runs so stale
        # runs never overwrite current. Fork PRs skip (step-summary only) so
        # fork code never gets write credentials. Publication failure fails
        # this step separately while the gate outcomes above stay preserved
        # in the summary.
        if: github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh --version
          jq --version
          gh --version | head -n 1 | grep -qE '^gh version ([2-9]|[1-9][0-9])\.'
          jq --version | grep -qE '^jq-(1\.([6-9]|[0-9]{2,})|[2-9])'
          head_repo="${{ github.event.pull_request.head.repo.full_name }}"
          base_repo="${{ github.repository }}"
          if [[ "$head_repo" != "$base_repo" ]]; then
            echo "Fork PR detected ($head_repo != $base_repo): step-summary only, no PR comment (fork code never gets write credentials)." >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi
          marker="dx-coverage-summary: seed linux_x86_64"
          pr="${{ github.event.pull_request.number }}"
          existing="$(gh api "repos/$base_repo/issues/$pr/comments?per_page=100" --jq "[.[] | select(.body | contains(\"$marker\")) | .id] | last // empty" || true)"
          if [[ -n "$existing" ]]; then
            jq -Rs '{body: .}' "${RUNNER_TEMP:-/tmp}/coverage_comment.md" > "${RUNNER_TEMP:-/tmp}/coverage_payload.json"
            gh api -X PATCH "repos/$base_repo/issues/comments/$existing" --input "${RUNNER_TEMP:-/tmp}/coverage_payload.json" --jq .id
            echo "Updated coverage summary comment $existing on PR #$pr." >> "$GITHUB_STEP_SUMMARY"
          else
            gh pr comment "$pr" --body-file "${RUNNER_TEMP:-/tmp}/coverage_comment.md"
            echo "Created coverage summary comment on PR #$pr." >> "$GITHUB_STEP_SUMMARY"
          fi
      - name: Prove coverage report contract (gate-owned LCOV, first-party comment)
        run: bazel run --noshow_progress //tools/ci:coverage_report_guards
      - name: Write job summary
        if: always()
        run: |
          echo "### coverage — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Command: dx coverage --min-coverage 97 //... (seed cell only; per-cell enforcement tracked in #89). First-party PR summary from the same seed LCOV (issue #254; Codecov at most opt-in)." >> "$GITHUB_STEP_SUMMARY"
  build-arm64:
    name: build-arm64 (bazel build //..., linux arm64 native)
    timeout-minutes: 60
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-arm64-
      - name: Build workspace
        run: bazel build --noshow_progress //...
      - name: Adopt-rust dx_dev smoke (local_path_override + dx_dev wiring, issue #407)
        run: bazel build --noshow_progress //examples/adopt-rust/... --config=dx_dev
      - name: Write job summary
        if: always()
        run: |
          echo "### build-arm64 — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Command: bazel build //... plus adopt-rust dx_dev smoke (linux arm64 native, local-only; issue #410)." >> "$GITHUB_STEP_SUMMARY"
  test-arm64:
    name: test-arm64 (bazel test //..., linux arm64 native)
    needs: [build-arm64]
    timeout-minutes: 60
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-arm64-
      - name: Test workspace
        run: bazel test --noshow_progress --flaky_test_attempts=3 --test_timeout=300 --local_test_jobs=4 //...
      - name: Report skip volume (Linux-only sh harness, issue #769)
        if: always()
        run: |
          echo "### skip volume — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Cell linux_arm64 real run, 0 skips: all 58 sh_test executed, no Linux-only skip (budget pinned by bazel run //tools/ci:skip_budget_qualification, issue #769)." >> "$GITHUB_STEP_SUMMARY"
      - name: Write job summary
        if: always()
        run: |
          echo "### test-arm64 — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Command: bazel test //... (linux arm64 native, local-only; manual targets excluded by Bazel tag semantics; issue #410)." >> "$GITHUB_STEP_SUMMARY"
  coverage-arm64:
    name: coverage-arm64 (dx coverage gate, arm64 cell)
    needs: [build-arm64]
    timeout-minutes: 60
    runs-on: ubuntu-24.04-arm
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-arm64-
      - name: Enforce arm64 coverage cell
        run: |
          set +e
          bazel run --noshow_progress //cli/cli:dx -- coverage --min-coverage 97 //... 2>&1 | tee "${RUNNER_TEMP:-/tmp}/dx_coverage_arm64.log"
          rc=${PIPESTATUS[0]}
          echo "$rc" > "${RUNNER_TEMP:-/tmp}/dx_coverage_arm64.rc"
          exit $rc
      - name: Render arm64 per-cell summary (same scope, no union)
        # Per-cell evidence with no cross-cell union (issue #410) plus the
        # per-cell visibility contract (issue #1062): rendered via
        # ./.github/actions/render-coverage-summary with cell arm64 linux_arm64
        # plus its versioned inventory. Same coverage_bin gate plus same
        # coverage_comment.sh shape as the seed cell; step-summary only by
        # design (only the seed publishes the PR comment, to avoid sixfold
        # spam).
        if: always()
        uses: ./.github/actions/render-coverage-summary
        with:
          cell: arm64 linux_arm64
          stem: dx_coverage_arm64
          inventory: tools/coverage/arm64-inventory.txt
      - name: Write job summary
        if: always()
        run: |
          echo "### coverage-arm64 — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Command: dx coverage --min-coverage 97 //... (arm64 cell only, per-cell no-union; issue #410)." >> "$GITHUB_STEP_SUMMARY"
  build-musl-x86_64:
    name: build-musl-x86_64 (static musl closure, linux x86_64)
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-musl-x86_64-
      - name: Prove Rust static-musl std resolves
        run: bazel query @rust_toolchains//... 2>/dev/null | grep -E -e 'musl' | LC_ALL=C sort -u
      - name: Build workspace (exec-platform tools)
        run: bazel build --noshow_progress //...
      - name: Write job summary
        if: always()
        run: |
          echo "### build-musl-x86_64 — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Command: bazel query musl toolchains plus bazel build //... (linux x86_64 static musl closure, local-only; exec-platform tools for build scripts/proc macros, target musl libs for apps; dynamic musl explicitly out of scope; issue #411)." >> "$GITHUB_STEP_SUMMARY"
  build-musl-arm64:
    name: build-musl-arm64 (static musl closure, linux arm64)
    timeout-minutes: 60
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-musl-arm64-
      - name: Prove Rust static-musl std resolves
        run: bazel query @rust_toolchains//... 2>/dev/null | grep -E -e 'musl' | LC_ALL=C sort -u
      - name: Build workspace (exec-platform tools)
        run: bazel build --noshow_progress //...
      - name: Write job summary
        if: always()
        run: |
          echo "### build-musl-arm64 — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Command: bazel query musl toolchains plus bazel build //... (linux arm64 static musl closure, local-only; exec-platform tools for build scripts/proc macros, target musl libs for apps; dynamic musl explicitly out of scope; issue #411)." >> "$GITHUB_STEP_SUMMARY"
  coverage-musl-x86_64:
    name: coverage-musl-x86_64 (dx coverage gate, musl x86_64 cell)
    needs: [build-musl-x86_64]
    timeout-minutes: 60
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-musl-x86_64-
      - name: Enforce musl x86_64 coverage cell
        run: |
          set +e
          bazel run --noshow_progress //cli/cli:dx -- coverage --min-coverage 97 //... 2>&1 | tee "${RUNNER_TEMP:-/tmp}/dx_coverage_musl_x86.log"
          rc=${PIPESTATUS[0]}
          echo "$rc" > "${RUNNER_TEMP:-/tmp}/dx_coverage_musl_x86.rc"
          exit $rc
      - name: Render musl x86_64 per-cell summary (same scope, no union)
        # Per-cell evidence with no cross-cell union (issue #411) plus the
        # per-cell visibility contract (issue #1062): rendered via
        # ./.github/actions/render-coverage-summary with cell musl-x86_64 linux_x86_64_musl
        # plus its versioned inventory. Same coverage_bin gate plus same
        # coverage_comment.sh shape as the seed cell; step-summary only by
        # design (only the seed publishes the PR comment).
        if: always()
        uses: ./.github/actions/render-coverage-summary
        with:
          cell: musl-x86_64 linux_x86_64_musl
          stem: dx_coverage_musl_x86
          inventory: tools/coverage/musl-x86_64-inventory.txt
      - name: Write job summary
        if: always()
        run: |
          echo "### coverage-musl-x86_64 — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Command: dx coverage --min-coverage 97 //... (musl x86_64 cell only, per-cell no-union; static musl only, dynamic out of scope; issue #411)." >> "$GITHUB_STEP_SUMMARY"
  coverage-musl-arm64:
    name: coverage-musl-arm64 (dx coverage gate, musl arm64 cell)
    needs: [build-musl-arm64]
    timeout-minutes: 60
    runs-on: ubuntu-24.04-arm
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-musl-arm64-
      - name: Enforce musl arm64 coverage cell
        run: |
          set +e
          bazel run --noshow_progress //cli/cli:dx -- coverage --min-coverage 97 //... 2>&1 | tee "${RUNNER_TEMP:-/tmp}/dx_coverage_musl_arm64.log"
          rc=${PIPESTATUS[0]}
          echo "$rc" > "${RUNNER_TEMP:-/tmp}/dx_coverage_musl_arm64.rc"
          exit $rc
      - name: Render musl arm64 per-cell summary (same scope, no union)
        # Per-cell evidence with no cross-cell union (issue #411) plus the
        # per-cell visibility contract (issue #1062): rendered via
        # ./.github/actions/render-coverage-summary with cell musl-arm64 linux_arm64_musl
        # plus its versioned inventory. Same coverage_bin gate plus same
        # coverage_comment.sh shape as the seed cell; step-summary only by
        # design (only the seed publishes the PR comment).
        if: always()
        uses: ./.github/actions/render-coverage-summary
        with:
          cell: musl-arm64 linux_arm64_musl
          stem: dx_coverage_musl_arm64
          inventory: tools/coverage/musl-arm64-inventory.txt
      - name: Write job summary
        if: always()
        run: |
          echo "### coverage-musl-arm64 — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Command: dx coverage --min-coverage 97 //... (musl arm64 cell only, per-cell no-union; static musl only, dynamic out of scope; issue #411)." >> "$GITHUB_STEP_SUMMARY"
  build-macos-arm64:
    name: build-macos-arm64 (bazel build //..., macOS arm64 native)
    timeout-minutes: 60
    runs-on: macos-14
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-macos-arm64-
      - name: Build workspace
        run: bazel build --noshow_progress //...
      - name: Adopt-rust dx_dev smoke (local_path_override + dx_dev wiring, issue #407)
        run: bazel build --noshow_progress //examples/adopt-rust/... --config=dx_dev
      - name: Write job summary
        if: always()
        run: |
          echo "### build-macos-arm64 — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Command: bazel build //... plus adopt-rust dx_dev smoke (macOS arm64 native on macos-14, local-only; pinned upstream toolchains, hermetic-llvm Apple-SDK backend provisional with no host-installed SDK fallback, no secrets, no interactive acceptance; issue #412)." >> "$GITHUB_STEP_SUMMARY"
  test-macos-arm64:
    name: test-macos-arm64 (bazel test //..., macOS arm64 native)
    needs: [build-macos-arm64]
    timeout-minutes: 60
    runs-on: macos-14
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-macos-arm64-
      - name: Test workspace
        run: bazel test --noshow_progress --flaky_test_attempts=3 --test_timeout=300 --local_test_jobs=4 //...
      - name: Report skip volume (Linux-only sh harness, issue #769)
        if: always()
        run: |
          echo "### skip volume — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Cell macos_arm64 budgeted 58 sh_test skips honestly (181 sh_binary build skips, 240 Linux-only labels; budget pinned by bazel run //tools/ci:skip_budget_qualification, issue #769)." >> "$GITHUB_STEP_SUMMARY"
      - name: Write job summary
        if: always()
        run: |
          echo "### test-macos-arm64 — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Command: bazel test //... (macOS arm64 native on macos-14, local-only; manual targets excluded by Bazel tag semantics; issue #412)." >> "$GITHUB_STEP_SUMMARY"
  coverage-macos-arm64:
    name: coverage-macos-arm64 (dx coverage gate, macos arm64 cell)
    needs: [build-macos-arm64]
    timeout-minutes: 60
    runs-on: macos-14
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-macos-arm64-
      - name: Enforce macos arm64 coverage cell
        run: |
          set +e
          bazel run --noshow_progress //cli/cli:dx -- coverage --min-coverage 97 //... 2>&1 | tee "${RUNNER_TEMP:-/tmp}/dx_coverage_macos_arm64.log"
          rc=${PIPESTATUS[0]}
          echo "$rc" > "${RUNNER_TEMP:-/tmp}/dx_coverage_macos_arm64.rc"
          exit $rc
      - name: Render macos arm64 per-cell summary (same scope, no union)
        # Per-cell evidence with no cross-cell union (issue #412) plus the
        # per-cell visibility contract (issue #1062): rendered via
        # ./.github/actions/render-coverage-summary with cell macos-arm64 macos_arm64
        # plus its versioned inventory. Same coverage_bin gate plus same
        # coverage_comment.sh shape as the seed cell; step-summary only by
        # design (only the seed publishes the PR comment).
        if: always()
        uses: ./.github/actions/render-coverage-summary
        with:
          cell: macos-arm64 macos_arm64
          stem: dx_coverage_macos_arm64
          inventory: tools/coverage/macos-arm64-inventory.txt
      - name: Write job summary
        if: always()
        run: |
          echo "### coverage-macos-arm64 — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Command: dx coverage --min-coverage 97 //... (macos arm64 cell only, per-cell no-union; host-installed SDK fallback never approved; issue #412)." >> "$GITHUB_STEP_SUMMARY"
  build-windows-x86_64:
    name: build-windows-x86_64 (bazel build //..., windows x86_64 native)
    timeout-minutes: 60
    runs-on: windows-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-windows-x86_64-
      - name: Build workspace
        run: bazel build --noshow_progress //...
      - name: Adopt-rust dx_dev smoke (local_path_override + dx_dev wiring, issue #407)
        run: bazel build --noshow_progress //examples/adopt-rust/... --config=dx_dev
      - name: Write job summary
        if: always()
        run: |
          echo "### build-windows-x86_64 — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Command: bazel build //... plus adopt-rust dx_dev smoke (windows x86_64 MSVC-compatible native on windows-latest with shell bash, local-only; pinned upstream toolchains, toolchains_msvc clang-cl/Microsoft-STL backend provisional with immutable lazy fetch plus explicit EULA never automatic, no installed fallback, no secrets; issue #414)." >> "$GITHUB_STEP_SUMMARY"
  test-windows-x86_64:
    name: test-windows-x86_64 (bazel test //..., windows x86_64 native)
    needs: [build-windows-x86_64]
    timeout-minutes: 60
    runs-on: windows-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-windows-x86_64-
      - name: Test workspace
        run: bazel test --noshow_progress --flaky_test_attempts=3 --test_timeout=300 --local_test_jobs=4 //...
      - name: Report skip volume (Linux-only sh harness, issue #769)
        if: always()
        run: |
          echo "### skip volume — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Cell windows_x86_64 budgeted 58 sh_test skips honestly (181 sh_binary build skips, 240 Linux-only labels; budget pinned by bazel run //tools/ci:skip_budget_qualification, issue #769)." >> "$GITHUB_STEP_SUMMARY"
      - name: Write job summary
        if: always()
        run: |
          echo "### test-windows-x86_64 — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Command: bazel test //... (windows x86_64 MSVC-compatible native on windows-latest with shell bash, local-only; manual targets excluded by Bazel tag semantics; issue #414)." >> "$GITHUB_STEP_SUMMARY"
  coverage-windows-x86_64:
    name: coverage-windows-x86_64 (dx coverage gate, windows x86_64 cell)
    needs: [build-windows-x86_64]
    timeout-minutes: 60
    runs-on: windows-latest
    defaults:
      run:
        shell: bash
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-windows-x86_64-
      - name: Enforce windows x86_64 coverage cell
        run: |
          set +e
          bazel run --noshow_progress //cli/cli:dx -- coverage --min-coverage 97 //... 2>&1 | tee "${RUNNER_TEMP:-/tmp}/dx_coverage_windows_x86_64.log"
          rc=${PIPESTATUS[0]}
          echo "$rc" > "${RUNNER_TEMP:-/tmp}/dx_coverage_windows_x86_64.rc"
          exit $rc
      - name: Render windows x86_64 per-cell summary (same scope, no union)
        # Per-cell evidence with no cross-cell union (issue #414) plus the
        # per-cell visibility contract (issue #1062): rendered via
        # ./.github/actions/render-coverage-summary with cell windows-x86_64 windows_x86_64
        # plus its versioned inventory. Same coverage_bin gate plus same
        # coverage_comment.sh shape as the seed cell; step-summary only by
        # design (only the seed publishes the PR comment).
        if: always()
        uses: ./.github/actions/render-coverage-summary
        with:
          cell: windows-x86_64 windows_x86_64
          stem: dx_coverage_windows_x86_64
          inventory: tools/coverage/windows-x86_64-inventory.txt
      - name: Write job summary
        if: always()
        run: |
          echo "### coverage-windows-x86_64 — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Command: dx coverage --min-coverage 97 //... (windows x86_64 cell only, per-cell no-union; toolchains_msvc backend provisional, explicit EULA never automatic, installed fallback never approved; issue #414)." >> "$GITHUB_STEP_SUMMARY"
  prove:
    name: prove (manual negatives, target tags, coverage cell, release hygiene, release policy, publish trust, shell contract, ci matrix, bootstrap, flakiness)
    needs: [build]
    timeout-minutes: 30
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-seed-
      - name: Prove battery (target tags, coverage, platforms, release, shell)
        # Includes //tools/ci:shell_contract via tools/ci/prove.sh.
        run: tools/ci/prove.sh
      - name: Write job summary
        if: always()
        run: |
          echo "### prove — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Proved: manual negatives (manual-tag interaction), target-tag semantics, seed coverage cell gate, coverage qualification (issue #308 per-cell plus Starlark plus Codecov plus quotas plus local-only remote; seed plus arm64 plus static musl plus macos arm64 plus windows x86_64 under #410/#411/#412/#414), static-musl qualification (issue #411 profiles plus exec/target plus per-cell plus dynamic out of scope), macOS arm64 qualification (issue #412 native plus per-cell plus portable-shell plus no host SDK fallback), Windows x86_64 qualification (issue #414 native MSVC-compatible plus per-cell plus portable-shell plus explicit EULA never automatic), CI multi-platform host matrix (issue #415 runners plus caches plus refusal plus sharding plus portable shell), bootstrap portability (issue #617 four hosts plus retry plus no sudo plus portable hash), CI flakiness plus timeout tuning (issue #619 retries plus timeouts plus sharding, long-timeouts-only rejected), release hygiene (issue #5 no-publication inputs), release policy (issue #89 curated defaults + parity gate), publish trust (issues #78/#26/#5 draft-only sites + nothing published), shell contract (issue #299 bash-only harness plus portable forms with windows shell bash, issue #414)." >> "$GITHUB_STEP_SUMMARY"
          echo "Proved (follow-up): coverage spill containment (issues #252/#656/#953, LLVM_PROFILE_FILE to scratch plus shared-library default)." >> "$GITHUB_STEP_SUMMARY"
  dogfood-freshness:
    name: dogfood-freshness (tools, generate --check, corpus audit)
    needs: [build]
    timeout-minutes: 30
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-dogfood-
      - name: Run dogfood-freshness battery (tools, freshness, audits)
        run: bazel run --noshow_progress //tools/ci:dogfood_freshness
      - name: Write job summary
        if: always()
        run: |
          echo "### dogfood-freshness — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Checked: Bazel-resolved tool execution, generate --check freshness, corpus ownership (carve-outs intact), code ownership (issue #12 lane A), non-dogfed execution plan (issue #508 explicit suites plus coverage-excluded runs plus shell ownership), examples READMEs (issue #85 commands + evidence), examples laziness (issue #85 no-install + isolation + deps-closure + action-graph + action-command zero-work), laziness-analysis (issue #930 configured-target plus action zero-delta 0/0/0 plus fetch-subset plus budgets held 10/10), quality cache aquery (issue #84 action-key isolation seed), depcheck contract (issue #22 truth table + open routes), wrapper sources (issues #12/#7/#8 QualitySourcesInfo), registry singularity (issues #6/#84 single-sourced taxonomy), backlog contracts (issues #9/#10 frozen pins + gap labels), GHCR hygiene (issue #460 separate workflow + gated push), widen-update loop contract (issue #260 never-rewrites + manager set + bump command + runner, delivered), quality foundations (issues #6/#7/#8/#12 provider + registry + mapping owners + battery contracts), distribution/closeout guards (issues #5/#26/#78/#460/#54 hygiene + workflows + no-publish invariants), backlog/automation guards (issues #9/#10/#85/#254/#260/#581 contracts + examples + coverage/native-only honesty), Supported-evidence gate (issue #301 no Supported without platform+consumer+release), quality adapters parity (issue #307 qualified, no false claims, provisional probes), env/codegen qualification (issue #309 deferred records plus fixture evidence, owned gaps), docs-pipeline qualification (issue #581 IR plus planning evidence, owned gaps, no working site), consumer-CI qualification (issue #312 contract plus fixture evidence, owned gaps), file-family qualification (issue #313 applicability plus fixture evidence, owned gaps), helper qualification (issue #315 per-helper decisions plus fixture evidence, owned gaps), clap-tokenizer qualification (issue #316 frozen legacy errors plus fixture evidence, owned gaps), hello-smoke qualification (issue #464 hello_output_test plus fixture evidence, owned gaps), parser-sample qualification (issue #465 per-adapter pass plus fail samples plus fixture evidence, owned gaps), rustfmt-edition qualification (issue #468 crate edition plus fixture evidence, owned gaps), CC opt-out qualification (issue #471 kept opt-out plus fixture evidence, owned gaps), cli-contract qualification (issue #457 final registry plus mutating-vs-check pins), rust-library qualification (issue #469 internal-only plus binaries-only boundary, owned gaps), CI multi-platform host matrix (issue #415 runners plus caches plus Windows refusal plus sharding plus portable shell).">> "$GITHUB_STEP_SUMMARY"
  devcontainer-check:
    name: devcontainer-check (init parity, definition valid)
    needs: [build]
    timeout-minutes: 15
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-bazelisk
      - name: Restore Bazel disk cache
        uses: ./.github/actions/restore-bazel-cache
        with:
          prefix: bazel-dogfood-
      - name: Prove dx init scaffold matches checked-in definition
        run: bazel test --noshow_progress --flaky_test_attempts=3 --test_timeout=300 //.devcontainer:devcontainer_parity_test
      - name: Validate devcontainer definition shape
        run: |
          python3 --version
          python3 -c 'import sys; assert sys.version_info >= (3, 9), sys.version'
          python3 - <<'EOF'
          import json
          with open(".devcontainer/devcontainer.json") as handle:
              definition = json.load(handle)
          post_create = definition["postCreateCommand"]
          assert "bazel run //dx:env" in post_create, post_create
          assert "bazel build //..." not in post_create, post_create
          print("devcontainer definition valid:", post_create)
          EOF
      - name: Write job summary
        if: always()
        run: |
          echo "### devcontainer-check — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Checked: dx init scaffold parity plus devcontainer definition shape (check-only; seed-host boot manually verified on Dockerfile change with non-Linux/arm64 wont-fix, no boot job in CI per docs/contributing/devcontainer.md#container-boot-manual)." >> "$GITHUB_STEP_SUMMARY"
  ci:
    name: ci
    # Single required status check for branch protection: aggregates every
    # job in this workflow so one green gate covers the whole matrix.
    # `if: always()` keeps the gate reporting when a need is cancelled or
    # fails; skipped needs (e.g. path-filtered) count as non-failing.
    if: ${{ always() }}
    needs:
      [
        docs-ci,
        dogfood,
        build,
        sbom,
        sbom-arm64,
        sbom-musl-x86_64,
        sbom-musl-arm64,
        sbom-macos-arm64,
        sbom-windows-x86_64,
        preset-stale,
        gitleaks,
        test,
        coverage,
        build-arm64,
        test-arm64,
        coverage-arm64,
        build-musl-x86_64,
        build-musl-arm64,
        coverage-musl-x86_64,
        coverage-musl-arm64,
        build-macos-arm64,
        test-macos-arm64,
        coverage-macos-arm64,
        build-windows-x86_64,
        test-windows-x86_64,
        coverage-windows-x86_64,
        prove,
        dogfood-freshness,
        devcontainer-check,
      ]
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - name: Aggregate job results
        env:
          NEEDS: ${{ toJson(needs) }}
        run: |
          python3 --version
          python3 -c 'import sys; assert sys.version_info >= (3, 9), sys.version'
          python3 - <<'EOF'
          import json
          import os
          import sys

          needs = json.loads(os.environ.get("NEEDS") or "{}")
          failing = sorted(
              key
              for key, value in needs.items()
              if value.get("result") not in ("success", "skipped")
          )
          print(json.dumps({"aggregate": "ci", "failing checks": failing}))
          sys.exit(1 if failing else 0)
          EOF
      - name: Write job summary
        if: always()
        run: |
          echo "### ci — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Aggregate required check over every ci.yml job (branch-protection single gate; skipped needs are non-failing)." >> "$GITHUB_STEP_SUMMARY"

# BEGIN qualification check strings — must be present for tools/ci/ci_matrix_qualification.sh
# Host matrix (issue #415): Linux x86_64 glibc (ubuntu-latest with shell bash); Linux arm64 glibc (supported, qualified under issue #410 — no hosted runner); macOS arm64 (macos-latest with shell bash); macOS x86_64 (supported, no hosted runner); Windows x86_64 MSVC-compatible native (windows-latest with shell bash, WSL/MSYS out of scope for bootstrap per issue #298 (closed; per-host successors own each host)
# Windows x86_64 MSVC-compatible native (windows-latest with shell bash,
# END qualification check strings
