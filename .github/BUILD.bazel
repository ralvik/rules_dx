"""Repository GitHub metadata corpus (M05 WP1)."""

load("//quality:fixtures.bzl", "real_source_target")

package(default_visibility = ["//visibility:public"])

# Repository corpus (M05 WP1): Markdown templates owned for direct-Bazel
# dogfood under the canonical real_fixture_policy. ci.yml has no quality
# class and stays outside the corpus.
real_source_target(
    name = "corpus",
    aspect_hints = ["//quality:corpus_vale_config"],
    markdown_srcs = glob(["**/*.md"]),
    starlark_srcs = ["BUILD.bazel"],
)
