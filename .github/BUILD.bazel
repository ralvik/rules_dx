"""Repository GitHub metadata corpus."""

load("//quality:fixtures.bzl", "real_source_target")

package(default_visibility = ["//visibility:public"])

# Consumer CI template harness (issue #99); see //tools/ci:consumer_scheduling_test.
# ci.yml stays out (no quality class); publish-dry-run.yml is a link sibling (issue #12).
exports_files([
    "workflows/publish-dry-run.yml",
    "workflows/reusable-consumer.yml",
])

real_source_target(
    name = "corpus_markdown",
    aspect_hints = ["//quality:corpus_vale_config"],
    markdown_siblings = [
        "//docs:contributing/local-workflows.md",
        "//docs:testing/README.md",
    ],
    markdown_srcs = [
        "ISSUE_TEMPLATE/bug_report.md",
        "ISSUE_TEMPLATE/feature_proposal.md",
        "PULL_REQUEST_TEMPLATE.md",
    ],
    tags = ["corpus"],
)

real_source_target(
    name = "corpus_starlark",
    starlark_srcs = ["BUILD.bazel"],
    tags = ["corpus"],
)
