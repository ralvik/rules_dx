"""Stable-stack compose fixture.

Freezes the exact current stable Bzlmod identities plus the LLVM
baseline-vs-target comparison with fixture evidence. The as-built stack
(Bazel 9.2.0 plus rules_rust 0.74.0 plus rules_cc 0.2.22 plus Rust 1.98.0)
composes on the seed host, proven by the Rust plus C++ hello fixtures;
the hermetic-llvm v0.8.19 candidate backend plus corpus wiring stays
provisional. Ad-hoc compose stays rejected.
See `docs/native-toolchains.md#qualification-questions-and-delivery`.
"""

load("//quality:fixtures.bzl", "real_source_target")

package(
    default_testonly = True,
    default_visibility = ["//:__subpackages__"],
)

exports_files([
    "pins.bzl",
])

real_source_target(
    name = "corpus_starlark",
    testonly = True,
    aspect_hints = ["//:buildifier_config"],
    starlark_srcs = [
        "BUILD.bazel",
        "pins.bzl",
    ],
    tags = ["corpus"],
)
