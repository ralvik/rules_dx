// CXX identity bridge source stub.
// Stands in for cxxbridge-cmd output until corpus wiring lands under.
#include "rust/tests/fixtures/cxx_identity/bridge.h"

int bridge_add(int left, int right) {
    return left + right;
}
