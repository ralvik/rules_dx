"""Authoritative Rust toolchain bindings (WP1)."""

load("//quality:fixtures.bzl", "real_source_target")
load(":identity_subject.bzl", "toolchain_identity_subject")
load(":identity_tests.bzl", "toolchain_identity_tests")

package(default_visibility = ["//visibility:public"])

toolchain_identity_subject(
    name = "identity_under_test",
)

toolchain_identity_tests(name = "identity")

test_suite(
    name = "all",
    tests = [":identity"],
)

real_source_target(
    name = "corpus_starlark",
    starlark_srcs = [
        "BUILD.bazel",
        "bindings.bzl",
        "identity_subject.bzl",
        "identity_tests.bzl",
    ],
    tags = ["corpus"],
)
