[project]
name = "hello"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = ["pytest>=7", "win-only==1.0.0; sys_platform == 'win32'"]

[project.optional-dependencies]
myfeat = ["optional-feat==1.0.0"]
