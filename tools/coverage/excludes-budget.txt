# LCOV excludes budget.
#
# Every source-level ignore needs a specific `reason:` plus `issue:`.
# Bare `policy:` without `reason:` plus `issue:` fails the gate.
#
# Scope: real `LCOV_EXCL_START`/`STOP`/`LINE` comment markers in tracked
# `*.rs`/`*.go`/`*.sh`, excluding intentional fail-closed fixtures
# (`cli/cli/src/reports/lcov.rs` bare test string,
# `cli/cli/src/exec/test_reports.rs` bare test string,
# `tools/ci/coverage_cell.sh` RANGE/bare fixtures) and pins constants
# (`cc/tests/fixtures/lcov_accounting/pins.bzl`,
# `tools/ci/lcov_accounting_qualification.sh` grep patterns).
# `cli/lcov` gate sources carry no contiguous markers by construction
# (test data splits the literal).
#
# Format: `<key>=<value>` per line; `#` comments skipped.
BUDGET_RANGES=60
BUDGET_LINES=28
BUDGET_TOTAL=148
EXPIRES=2027-03-01
ISSUE=1055
