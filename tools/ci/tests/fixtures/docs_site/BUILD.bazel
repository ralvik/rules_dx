"""Docs site execution plus rebuild plus link-completeness plus guide plus timing plus drift fixture."""

load("//quality:fixtures.bzl", "real_source_target")

package(
    default_testonly = True,
    default_visibility = ["//:__subpackages__"],
)

exports_files([
    "pins.bzl",
    "docs_site.expected",
    "rebuild.expected",
    "links.expected",
    "guide.expected",
    "timing.expected",
    "drift.expected",
])

real_source_target(
    name = "corpus_starlark",
    testonly = True,
    aspect_hints = ["//:buildifier_config"],
    starlark_srcs = [
        "BUILD.bazel",
        "pins.bzl",
    ],
    tags = ["corpus"],
)
