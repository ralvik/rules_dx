Release packaging for artifact-into-releases (issue #813).
Packaged unit: //deploy/release:release_artifacts builds curator plus binary
plus man page plus NOTICE plus SBOM/provenance (//:audit_curator declares
licenses.toml bytes, //cli/cli:dx seed binary, //cli/cli:man_pages ships
man/dx.1, :notice_demo hermetic notice_bundle with byte-identical rebuilds,
:sbom_demo SPDX-2.3 plus SLSA v1 with subject digest equal to artifact
sha256, hermetic Rust toolchain only, bazel test
//deploy/release:dx_release_tools_test binds SPDX plus in-toto plus SLSA).
NOTICE: hermetic notice_bundle in deploy/release/notice.bzl over audited
inventory with deterministic bytes plus byte-identical rebuilds,
missing-notice-text fails the action, notice_verify_files plus dx_verify
--notice before install, signed alongside the SBOM pair via
//deploy/release:signing_demo. Pipeline: publish-dry-run.yml builds
//deploy/release:release_artifacts, stages SPDX plus provenance plus NOTICE
under RUNNER_TEMP/publish-dry-run, signing dry-run log covers
sbom_demo.spdx.json plus sbom_demo.provenance.json plus notice_demo.NOTICE,
dry-run-report.json records packaging plus sbom plus notice with published
False, step summary records packaging plus SBOM linkage plus NOTICE,
dist/release stay git-ignored with staging under RUNNER_TEMP and
clean-checkout proof. Driver: release_driver dry run names release_artifacts
before notice_demo before signing_demo before github_draft. Attestation stays
owner-gated human-run via //deploy/release:signing_demo (Sigstore keyless
cosign sign-blob --bundle plus gh attestation create on
tuf-repo-cdn.sigstore.dev); CI never signs PR code, fork-safe, publishes
nothing. Untracked workspace read rejected, declared Bazel inputs only;
checksum-only verification is not publisher-identity proof; unsigned draft or
publish rejected, signing-first; Compatibility: Release only. Qualified
seed-only under issue #813; wider matrix plus tag cut stays owned gap under
process #808; no Supported claim.
