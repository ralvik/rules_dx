Release matrix follow-ups qualified (issue #815).
Matrix: dx-linux-x86_64 qualified-seed-built-here via publish dry-run plus
dx-linux-arm64 qualified-host-evidence plus dx-macos-arm64
qualified-host-evidence plus dx-windows-x86_64 qualified-host-evidence,
frozen four-cell order with seed first (macOS x86_64 Not planned per #976
with no cell), release_matrix_unqualified() empty with no
unqualified-per-issue-311 remains. Per-host platform evidence:
Platform-qualified Linux arm64 native under issue #410 plus macOS arm64
native under issue #412 plus Windows x86_64 MSVC-compatible native under
issue #414. Per-host release
evidence: linux_arm64 sbom-provenance delivered under issue #803 plus
macos_arm64 sbom-provenance delivered under issue #805 plus
windows_x86_64 sbom-provenance delivered under issue #807; every
required host landed under closed #803 plus #805 plus #807,
process #808.
Workflow: publish-dry-run.yml carries the same qualified matrix, seed-only
build with non-seed cells human-run only, published False plus submitted
False everywhere. Docs: authoring plus runbook plus CLI build record the
qualified matrix with per-host evidence. Draft-only ceiling enforced plus
owner approval required plus module 0.0.0 with no v* tags; checksum-only
verification is not publisher-identity proof; Compatibility: Release only.
Qualified under issue #815; tag cut stays owned gap under process #808; no Supported claim.
