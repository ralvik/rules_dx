"""Focused C# environment plans.

Each `csharp_env_plan` reads the analyzed authoritative providers of one
`dx_csharp_*` wrapper: `QualitySourcesInfo` for direct sources plus the
preserved `DotnetAssembly*` toolchain closure by reference.
Focused-target only; repository/root/exact-target orchestration remains M25.
"""

load("//csharp/env:plan.bzl", "csharp_env_plan")
load("//csharp/env:plan_tests.bzl", "env_plan_tests")
load("//quality:fixtures.bzl", "real_source_target")

package(default_visibility = ["//visibility:public"])

csharp_env_plan(
    name = "hello_lib_plan",
    target = "//csharp/hello:hello_lib",
)

env_plan_tests(
    name = "env_plan_tests",
    subjects = [
        ":hello_lib_plan",
    ],
)

# Repository corpus: plan Starlark owned for direct-Bazel dogfood
# under the canonical real_fixture_policy.
real_source_target(
    name = "corpus",
    starlark_srcs = [
        "BUILD.bazel",
        "plan.bzl",
        "plan_tests.bzl",
    ],
)
