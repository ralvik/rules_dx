"""CSharpier format fixture."""

load("@rules_dx//csharp/rules:defs.bzl", "csharp_library")
load("//quality:fixtures.bzl", "real_source_target")

package(
    default_testonly = True,
    default_visibility = ["//:__subpackages__"],
)

exports_files([
    "pins.bzl",
    "Sample.cs",
    "check_dirty.txt",
])

real_source_target(
    name = "corpus_starlark",
    testonly = True,
    aspect_hints = ["//:buildifier_config"],
    starlark_srcs = [
        "BUILD.bazel",
        "pins.bzl",
    ],
    tags = ["corpus"],
)

csharp_library(
    name = "csharpier",
    testonly = True,
    srcs = ["Sample.cs"],
)
