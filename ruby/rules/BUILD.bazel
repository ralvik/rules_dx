"""Experimental minimal Ruby wrappers (ADR 0032)."""

load("//quality:fixtures.bzl", "real_source_target")
load(":wrapper_tests.bzl", "ruby_wrapper_contract_tests")

package(default_visibility = ["//visibility:public"])

exports_files([
    "defs.bzl",
    "wrapper_tests.bzl",
])

# Corpus: BUILD ownership; see docs/contributing/build-conventions.md.
ruby_wrapper_contract_tests(name = "wrapper_contract_tests")

real_source_target(
    name = "corpus_starlark",
    aspect_hints = ["//:buildifier_config"],
    starlark_srcs = [
        "BUILD.bazel",
        "defs.bzl",
        "wrapper_tests.bzl",
    ],
    tags = ["corpus"],
)
