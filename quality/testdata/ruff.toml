# Hinted Ruff policy for the real-pipeline fixture (WP2, strict per
# . Pins the strict lint selection explicitly so the hinted
# run proves the config closure reaches the tool instead of `--isolated`
# defaults (same selection as the workspace `ruff.toml`).
[lint]
select = ["E4", "E7", "E9", "F", "W", "I", "B"]
