<?xml version="1.0"?>
<!DOCTYPE module PUBLIC "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN" "https://checkstyle.org/dtds/configuration_1_3.dtd">
<!-- Minimal Checkstyle policy for the Layer-2 matrix (unused imports only).
Mirrors the `vale_cfg`/`ruff_cfg` hinted closures: the config reaches the
tool as `-c`, never as an ambient default. -->
<module name="Checker">
  <module name="TreeWalker">
    <module name="UnusedImports"/>
  </module>
</module>
