# Hinted Vale policy for the real-pipeline fixture (strict per issue
# , same Dx.Markers style as the workspace corpus). The [*]
# BasedOnStyles binding is required; without it Vale reports {}.
StylesPath = styles
MinAlertLevel = suggestion

[*]
BasedOnStyles = Dx
