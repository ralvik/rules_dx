# Strict own-tree Vale policy (issue #614): Dx.Markers enforced. The
# [*] BasedOnStyles binding is required; without it Vale runs with no
# styles and reports {} even on TODO markers.
StylesPath = corpus_styles
MinAlertLevel = suggestion

[*]
BasedOnStyles = Dx
