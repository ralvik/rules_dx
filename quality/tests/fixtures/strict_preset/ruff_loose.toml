# Loose Ruff policy example (default).
#
# Mirrors the repository loose pin (E4, E7, E9, F): the default-equivalent
# selection used without a strict opt-in. Pinned upstream defaults stay
# authoritative for omitted settings; this file owns only the lint
# selection.
[lint]
select = ["E4", "E7", "E9", "F"]
