
load("@rules_dx//ruby/rules:defs.bzl", "ruby_library")
load("//quality:fixtures.bzl", "real_source_target")

package(
    default_testonly = True,
    default_visibility = ["//:__subpackages__"],
)

exports_files([
    "pins.bzl",
    "Sample.rb",
    "check_findings.json",
])

real_source_target(
    name = "corpus_starlark",
    testonly = True,
    aspect_hints = ["//:buildifier_config"],
    starlark_srcs = [
        "BUILD.bazel",
        "pins.bzl",
    ],
    tags = ["corpus"],
)

ruby_library(
    name = "rubocop",
    testonly = True,
    srcs = ["Sample.rb"],
)
