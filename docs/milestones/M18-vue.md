# M18: Vue

## Outcome

Vue single-file components have an adapter-tested application, generation, test/coverage, quality,
IDE, and provider-derived environment-plan path for focused targets.

## Scope

Implement a Vue-specific adapter over its pinned upstream parser/compiler and Bazel integration, preserving
one physical owner and exact script, template, style, dependency, generated-output, test/coverage,
quality-region, and focused environment projection semantics. Public repository/root/exact-target env
orchestration remains in M25.

## Contract References

- [Generation](../generation/), [quality](../quality/), [environments](../environments/), [tools](../tools/), [support matrix](../product/support-matrix.md), [testing](../testing/), and open decision [O29](../open-decisions.md).

## Deliverables

- Vue wrappers/provider normalization, Gazelle adapter, quality classification, and provider-derived
  environment/IDE plan and focused projection.
- Vue build, test, coverage, run, strict dependency, merge, platform, and laziness fixtures.

## Work Packages

1. Prove the stable upstream Vue parser/compiler, package, provider, and target boundary.
2. Implement physical ownership, virtual-region hand-off, generation, execution, test/coverage,
   provider-derived environment plans/projections, and quality integration.
3. Certify stale cleanup, collisions, unsupported syntax, and unused-adapter laziness.

## Milestone-Specific Evidence

- No regex extraction, plain-JS fallback, duplicate region ownership, or compiler execution during generation occurs.
- Vue fixtures prove build, test, coverage ownership/completeness, and focused environment projection
  for every applicable support-matrix cell independently before any mixed-framework claim.

## Out Of Scope

- Svelte, Astro, MDX, generic container APIs, and supported status.

## Completion Report Additions

- Record upstream stack, supported Vue syntax/targets, provider mappings, regions, platforms, and limitations.
