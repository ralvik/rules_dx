"""Repository documentation corpus."""

load("//quality:fixtures.bzl", "real_source_target")

package(default_visibility = ["//visibility:public"])

# Repository corpus: all Markdown under docs/ owned for direct-Bazel
# dogfood under the canonical real_fixture_policy. The root pages, the
# linked behavioral matrix, quality/result.proto, the documentation IR
# schema plus codec source, and the publish dry-run workflow linked from
# docs/deploy/authoring.md are unclassified link-resolution siblings,
# never checked sources.
real_source_target(
    name = "corpus",
    aspect_hints = ["//quality:corpus_vale_config"],
    markdown_siblings = [
        "//.github:workflows/publish-dry-run.yml",
        "//:AGENTS.md",
        "//:CHANGELOG.md",
        "//:CONTRIBUTING.md",
        "//:README.md",
        "//:SECURITY.md",
        "//docs/ir:doc_ir.proto",
        "//docs/ir/ir:src/lib.rs",
        "//libs/starlark:behavioral_matrix.md",
        "//quality:result.proto",
    ],
    markdown_srcs = glob(["**/*.md"]),
    starlark_srcs = ["BUILD.bazel"],
)
